---
status: partial
phase: 1015-demo-showcase-workspace
source:
  - 1015-01-SUMMARY.md
  - 1015-02-SUMMARY.md
  - 1015-03-SUMMARY.md
  - 1015-04-SUMMARY.md
started: 2026-04-23T17:01:37Z
updated: 2026-04-23T19:34:12Z
---

## Current Test

[testing complete — new blocker: widgets do not render in dashboard page content area]

## Tests

### 1. Cold Start Smoke Test
expected: From a fresh MATLAB session with no timers running, `install(); ctx = run_demo();` boots without errors, opens the dashboard figure, starts the synthetic data timer, and returns ctx populated with engine/pipeline/store/writerTimer/plantHealthKey.
result: issue
reported: "Dashboard figure opens and 6 page tabs render. Detached 'Reactor Pressure (live)' figure opens in its own window and updates (sine wave visible). BUT the main dashboard content area below the tab bar is entirely blank on the Overview page — no widgets visible, no plant.health rollup, no KPIs, nothing rendering or updating. Only the tab header and chrome (FIGURE/FORMAT/TOOLS/LAYOUT menu, 'Last update: 19:34:12', Sync/Live/Edit/Save/Image/Export buttons, From/To range sliders showing year 5182-5186) are shown. Entire widget grid area is black."
severity: blocker

### 2. Six Pages Present
expected: The dashboard toolbar / page switcher shows exactly six pages: Overview, Feed Line, Reactor, Cooling, Events, Diagnostics. Clicking each one switches content.
result: pass
evidence: "Screenshot 2026-04-23: dashboard tab bar shows exactly 6 tabs — Overview, Feed Line, Reactor, Cooling, Events, Diagnostics. Overview tab highlighted as active. Tab switching not verified separately due to Test 1 blocker (empty content areas)."

### 3. All 20 Widget Kinds Render
expected: Across the six pages, every widget kind renders without placeholder / error state — fastsense, rawaxes, number, status, gauge, multistatus, text, table, barchart, heatmap, histogram, scatter, image, eventtimeline (timeline), group, divider, collapsible, iconcard, chipbar, sparklinecard.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 new blocker — widget content area does not render. Requires fix before this test can be validated."

### 4. plant.health Rollup on Overview
expected: Overview page has a prominent StatusWidget driven by the plant.health CompositeTag. It displays a finite numeric state (green/healthy at idle) and updates live as the synthetic generator ticks.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 new blocker — widget content area does not render. Requires fix before this test can be validated."

### 5. Reactor Pressure Pre-Detached on Startup
expected: On boot, the reactor-pressure FastSenseWidget opens as its own detached figure window, separate from the main dashboard figure. The detached plot is live-mirrored and updates in sync.
result: pass
evidence: "Screenshot 2026-04-23: 'Reactor Pressure (live) — Live' tab visible in MATLAB Figures window, separate from dashboard figure. Time-series plot shows sine-like pressure curve ~11-13 bar with one spike ~19 bar, live-updating. Note: mirror-to-dashboard verification is partial — dashboard content area is blank (see Test 1), so 'in sync' cannot be fully verified until Test 1 is fixed."

### 6. Event Timeline Shows Critical Events
expected: After ~30s of runtime (enough for the 15s anomaly window), the Events page timeline widget displays at least one reactor.pressure.critical event, and the adjacent FastSense plot shows round event markers at the same timestamps.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 new blocker — widget content area does not render. Requires fix before this test can be validated."

### 7. Detach Button Works on Other Widgets
expected: Click the detach button (info / detach control) on any non-pre-detached FastSenseWidget. It pops out into its own figure, stays live-mirrored to the dashboard, and closing the pop-out leaves the dashboard untouched.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 new blocker — widget content area does not render. Requires fix before this test can be validated."

### 8. Reactor Advanced Collapsible Expands
expected: On the Reactor page, an 'Advanced' collapsible section (GroupWidget in collapsible mode) toggles open to reveal a FastSense plot of reactor.rpm. Toggling collapses it again.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 new blocker — widget content area does not render. Requires fix before this test can be validated."

### 9. Headless Test Suite Passes
expected: Running `tests/run_all_tests.m` (Octave or MATLAB) completes with 77/77 (or all) tests green, including `test_demo_industrial_plant_smoke` (skips cleanly on Octave without MATLAB `timer`) and the five headless assertions in `TestDemoIndustrialPlantHeadless`.
result: pass
evidence: "Octave 11.1.0: `=== Results: 77/77 passed, 0 failed ===` — run 2026-04-23. test_demo_industrial_plant_smoke skipped cleanly on Octave as designed. Plus 1015-04 added test_multistatus_monitortag_bare (77 → 78 tests, 1 pre-existing unrelated full-suite segfault in test_add_marker)."

### 10. Clean Teardown, No Dangling Timers
expected: After calling `teardownDemo(ctx)` (or closing the dashboard figure), `numel(timerfindall())` returns to its pre-boot value. No zombie timers, no open figures left behind.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 new blocker — widget content area does not render. Requires fix before this test can be validated."

## Summary

total: 10
passed: 3
issues: 1
pending: 0
skipped: 0
blocked: 6

## Gaps

- truth: "Dashboard page content area renders widgets and updates live"
  status: failed
  reason: "User reported on MATLAB re-test: dashboard boots (no MultiStatusWidget crash — 1015-04 fix held), 6 tabs render correctly, pre-detached Reactor Pressure figure renders and updates. BUT the main dashboard page content area below the tab bar is ENTIRELY BLANK on the Overview page — no widgets visible at all, nothing updating. Only the chrome is shown (title, menu, buttons, From/To range sliders showing year 5182-5186 which suggests time axis misconfiguration). Widget grid region is solid black."
  severity: blocker
  test: 1
  artifacts:
    - libs/Dashboard/DashboardEngine.m
    - libs/Dashboard/DashboardLayout.m
    - libs/Dashboard/DashboardBuilder.m
    - demo/industrial_plant/private/buildOverviewPage.m
    - demo/industrial_plant/private/buildDashboard.m
  missing:
    - Widgets rendering in page content area (tab-switch / page activation wiring)
    - Correct time axis initialization (From/To shows year 5182 — datenum vs posix confusion?)
    - Live refresh driving widget updates on the active page
  observations:
    - "From/To slider shows '5182-03-13 21:51' to '5186-05-12 06:18' — extreme future dates suggest posix seconds being interpreted as datenum, or datenum being interpreted as posix, somewhere in the time-axis/range setup."
    - "Detached figure DOES update, so the pipeline and LiveTagPipeline writer timer are running. The break is between pipeline and in-dashboard widget realization/refresh."

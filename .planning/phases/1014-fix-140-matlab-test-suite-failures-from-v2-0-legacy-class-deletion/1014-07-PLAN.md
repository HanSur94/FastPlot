---
phase: 1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion
plan: 07
type: execute
wave: 3
depends_on: [1014-01, 1014-02, 1014-03, 1014-04, 1014-05, 1014-06]
files_modified:
  - tests/suite/TestTag.m
  - tests/suite/TestToolbar.m
  - tests/suite/TestMonitorTagEvents.m
autonomous: false
requirements: [PHASE-1014-GATE]
must_haves:
  truths:
    - "All Category F residual verify-failures triaged and fixed or deleted"
    - "Full MATLAB suite (scripts/run_tests_with_coverage.m) runs green locally"
    - "Full Octave suite (tests/run_all_tests.m) runs green"
    - "MISS_HIT full lint sweep (libs/ tests/ examples/) clean"
    - "CI job `Tests → MATLAB Tests` returns success on push to topic branch"
    - "Zero legacy-class constructor calls across the entire tests/suite/ tree"
    - "Zero testCase.TestData references across tests/suite/"
    - "Zero detectEventsFromSensor references across tests/suite/"
  artifacts:
    - path: "tests/suite/TestTag.m"
      provides: "Tag constructor-contract test aligned with current Tag.m signature"
    - path: "tests/suite/TestToolbar.m"
      provides: "Toolbar tests aligned with current FastSenseToolbar structure"
    - path: "tests/suite/TestMonitorTagEvents.m"
      provides: "MonitorTag event-emission tests aligned with current MONITOR-05 carrier pattern"
  key_links:
    - from: "CI workflow"
      to: "MATLAB Tests job green"
      via: "push to topic branch + gh run watch"
      pattern: "MATLAB Tests.*success"
---

<objective>
Wave 3 Category F residuals + FINAL VERIFICATION. The long tail of verify-failures not addressed by Waves 0-2. Plus the phase exit gate: push to CI and confirm `MATLAB Tests` goes green.

**Scope:**
1. `TestTag/testConstructorRequiresKey` (1 verify-fail) — likely Tag.m constructor signature drift.
2. `TestToolbar/*` (3 verify-fails) — FastSenseToolbar UI changes.
3. `TestMonitorTagEvents/testCarrierPatternNoTagKeys` (1 verify-fail) — MONITOR-05 carrier text drift.
4. Any residuals surfaced by Waves 0-2 that were not caught in their plans (sanity pass).
5. **Final verification gate:** full MATLAB + Octave + MISS_HIT sweep + CI push.

**Non-autonomous:** this plan has a `checkpoint:human-verify` task at the end for the CI confirmation step — the human confirms they saw the `MATLAB Tests` job return green after `git push`. (Everything else is fully automated.)

Purpose: Close the phase. Output: 3 files fixed, phase-level green confirmation, phase gate CI result captured in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md
@tests/suite/TestTag.m
@tests/suite/TestToolbar.m
@tests/suite/TestMonitorTagEvents.m
@libs/SensorThreshold/Tag.m
@libs/SensorThreshold/MonitorTag.m
@libs/FastSense/FastSenseToolbar.m

<interfaces>
<!-- Tag.m constructor (libs/SensorThreshold/Tag.m ~line 80): -->
<!--   Tag('key') — key must be non-empty char; errors 'Tag:missingKey' -->

<!-- MonitorTag MONITOR-05 carrier pattern: -->
<!--   After Phase 1010, Event.TagKeys exists; MonitorTag.m docstrings reference -->
<!--   "per-Tag keys field" in abstract terms (per STATE.md phase 1006 decisions). -->

<!-- FastSenseToolbar current button count / structure in libs/FastSense/FastSenseToolbar.m -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix TestTag, TestToolbar, TestMonitorTagEvents residuals</name>
  <files>tests/suite/TestTag.m, tests/suite/TestToolbar.m, tests/suite/TestMonitorTagEvents.m</files>
  <read_first>
    - tests/suite/TestTag.m (specifically `testConstructorRequiresKey` — 1 verify-fail per CI log)
    - tests/suite/TestToolbar.m (3 verify-fails; RESEARCH triage table names `testToolbarHasAllButtons`, `testExportPNG`, and one unnamed third)
    - tests/suite/TestMonitorTagEvents.m (1 verify-fail: `testCarrierPatternNoTagKeys`)
    - libs/SensorThreshold/Tag.m lines 1-120 (abstract base; constructor signature + error IDs)
    - libs/SensorThreshold/MonitorTag.m (current docstring / property declarations — MONITOR-05 carrier pattern references)
    - libs/FastSense/FastSenseToolbar.m (current button list, export paths)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md (D-02-F — per-test root cause, no bulk rule)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Category F Triage Table)
  </read_first>
  <action>
Per-file per-method triage. Process all three files:

**TestTag/testConstructorRequiresKey:**
1. Read the test body.
2. Read `libs/SensorThreshold/Tag.m` constructor (~line 80). RESEARCH says Tag errors on empty/non-char key with `Tag:missingKey` or similar.
3. Probably the test calls `Tag()` with no args expecting an error — but `Tag` is abstract (STATE.md Phase 1004: throw-from-base with `Tag:notImplemented`). The correct probe is `MockTag(...)` (see `tests/suite/MockTag.m`). Update the test to:
   - use `MockTag` instead of `Tag` for instantiation
   - or assert on the right error ID (`Tag:missingKey` vs `Tag:emptyKey` — check actual source)
4. Fix the test. Commit: `fix(1014-test): TestTag/testConstructorRequiresKey — align with Tag base contract`

**TestToolbar/* (3 failures):**
1. Run `matlab -batch "runtests('tests/suite/TestToolbar.m')"` — observe which 3 of 19 methods fail.
2. Per RESEARCH triage:
   - `testToolbarHasAllButtons`: hardcoded `verifyEqual(numel(children), 11)` probably — count is current actual count. Read `libs/FastSense/FastSenseToolbar.m` for current number; update expected.
   - `testExportPNG`: headless regression analog — if `print()` path, confirm `exportgraphics` branch wiring in FastSenseToolbar. If it calls an authoritative `exportImage` that already uses `exportgraphics`, the test's expected file format might have changed. Update.
   - Third unnamed: diagnose from test output. Likely another UI-count assertion.
3. Do NOT add library changes unless the test surfaces a real product bug beyond the authorized Plan 01 scope (D-05 — STOP and re-discuss).
4. Commit: `fix(1014-test): TestToolbar — update UI-drift expectations for 3 methods`

**TestMonitorTagEvents/testCarrierPatternNoTagKeys:**
1. Read the test body — it probably greps MonitorTag.m for specific carrier-pattern text.
2. STATE.md Phase 1006 decision: MonitorTag.m docstrings reference "per-Tag keys field" in abstract terms (not literal `Event.TagKeys` — Pitfall 5 gate). The test's regex may have drifted.
3. Read current `libs/SensorThreshold/MonitorTag.m` docstring/code; update the test's regex or assertion to match.
4. Commit: `fix(1014-test): TestMonitorTagEvents/testCarrierPatternNoTagKeys — update regex for current MonitorTag docstring`

**Sanity sweep — catch anything Waves 0-2 missed:**
After the three individual commits, run:
```
matlab -batch "addpath('scripts'); run_tests_with_coverage()"
```
If ANY test errors or verify-fails remain (from any file): triage per D-02-A (delete or migrate) and commit separately. Each residual gets its own commit.

**D-05:** target 5-10 min per method. Do not overshoot.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); for T = {'TestTag','TestToolbar','TestMonitorTagEvents'}; runtests(['tests/suite/' T{1} '.m']); end" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - `matlab -batch "runtests('tests/suite/TestTag.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestToolbar.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestMonitorTagEvents.m')" ; echo $?` returns 0
    - `mh_style` + `mh_lint` on the 3 files exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - `git diff --name-only | grep -E "^libs/"` returns empty (no library changes in this plan)
    - 3 commits landed (plus any sanity-sweep commits for residuals)
  </acceptance_criteria>
  <done>
3 residual files fixed. No unauthorized libs/ changes. Sanity sweep across full suite captures any Wave 0-2 misses. Commits landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full-suite green gate — MATLAB + Octave + MISS_HIT</name>
  <files>(no file edits — verification task)</files>
  <read_first>
    - scripts/run_tests_with_coverage.m
    - tests/run_all_tests.m
    - miss_hit.cfg
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md (D-03 verification strategy)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-VALIDATION.md (sign-off criteria)
  </read_first>
  <action>
Run the three gate commands in sequence, capturing output:

STEP 1 — MATLAB full suite:
```
matlab -batch "addpath('scripts'); run_tests_with_coverage()"; echo "MATLAB_EXIT=$?"
```
If EXIT != 0, grep the output for the specific failures. Go back and fix them in a follow-up commit attributed to the right Plan number (02-06 scope) OR add a residual commit under Plan 07.

STEP 2 — Octave full suite (function-style tests):
```
octave --eval "cd tests; run_all_tests()"; echo "OCTAVE_EXIT=$?"
```
If EXIT != 0, this is a REGRESSION caused by Phase 1014 library edit (DashboardBuilder.m in Plan 01). **STOP per CONTEXT.md D-05** ("If fixes to `libs/` cascade beyond the two explicitly-authorised files in Category E, STOP and re-discuss — that means the scope was mis-categorised"). Surface the regression to the user with: the failing Octave test name, the exact DashboardBuilder.m line/diff from Plan 01, the Octave stack trace. **Do NOT edit any libs/ file** before the user confirms scope expansion. If the user approves a follow-up lib edit, commit as `fix(1014-lib): DashboardBuilder.exitEditMode — Octave regression follow-up` (only after explicit approval).

STEP 3 — MISS_HIT lint sweep:
```
mh_style libs/ tests/ examples/ && mh_lint libs/ tests/ examples/ && mh_metric --ci libs/ tests/ examples/; echo "LINT_EXIT=$?"
```
If EXIT != 0, identify which file triggered — if it's a Phase 1014-touched file, fix inline (NO suppressions per D-04). If it's an unrelated file, note in SUMMARY but don't fix (out of scope).

STEP 4 — Phase-level grep gates (must all return 0):
```
grep -rE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/ | wc -l
grep -rE "detectEventsFromSensor\s*\(" tests/suite/ | wc -l
grep -rE "testCase\.TestData" tests/suite/ | wc -l
```
All three must return 0. If any is nonzero, identify the offending file and route back to the owning plan (02-06) as a follow-up commit under this plan.

STEP 5 — Commit a phase-exit marker if any follow-up fixes landed in this task:
- `fix(1014): phase-exit full-suite fixups`

No commit needed if all gates passed first time.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); run_tests_with_coverage()" && octave --eval "cd tests; run_all_tests()" && mh_style libs/ tests/ examples/ && mh_lint libs/ tests/ examples/ && mh_metric --ci libs/ tests/ examples/ ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - `matlab -batch "addpath('scripts'); run_tests_with_coverage()" ; echo $?` returns 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - `mh_style libs/ tests/ examples/ && mh_lint libs/ tests/ examples/ && mh_metric --ci libs/ tests/ examples/ ; echo $?` returns 0
    - `grep -rE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/ | wc -l` returns 0
    - `grep -rE "detectEventsFromSensor\s*\(" tests/suite/ | wc -l` returns 0
    - `grep -rE "testCase\.TestData" tests/suite/ | wc -l` returns 0
  </acceptance_criteria>
  <done>
All three local gates (MATLAB, Octave, MISS_HIT) are green. All three phase-level grep gates return 0. Any necessary fixup commits landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Push to CI and verify MATLAB Tests green (human confirmation)</name>
  <read_first>
    - .github/workflows/tests.yml (MATLAB Tests job + Octave Tests job)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md (D-03 — CI is authoritative)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-VALIDATION.md (sign-off criteria)
  </read_first>
  <what-built>
    All 7 plans of Phase 1014 have landed their commits. Local gates (MATLAB + Octave + MISS_HIT + grep gates) are green. Now the authoritative signal: push the current topic branch to GitHub and watch `Tests → MATLAB Tests` turn green on the Actions runner.
  </what-built>
  <how-to-verify>
    1. Confirm current branch is a topic branch (NOT main/master):
       ```
       git branch --show-current
       ```
       Expected: `claude/vigorous-shannon-7b38c7` or similar topic branch name.

    2. Push the branch:
       ```
       git push origin $(git branch --show-current)
       ```

    3. Watch the CI run via gh CLI:
       ```
       gh run list --workflow "Tests" --branch $(git branch --show-current) --limit 1
       gh run watch
       ```

    4. Confirm EACH job returns success:
       - `Tests → MATLAB Tests` (R2020b): GREEN
       - `Tests → Octave Tests`: GREEN
       - `Tests → MISS_HIT` (or equivalent lint job): GREEN
       - `Tests → MEX Build matrix`: GREEN (if run)

    5. If any job fails:
       - Capture the error output: `gh run view <run-id> --log-failed | head -200`
       - Diagnose the CI-only regression (could be R2020b edge case not caught locally).
       - Fix and push again; re-watch.

    6. Once `MATLAB Tests` is green, update `.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-VALIDATION.md`:
       - Set `status: approved`
       - Fill in `**Approval:** approved YYYY-MM-DD`

    7. Type "approved" or "CI green" to confirm you saw the green result.
  </how-to-verify>
  <acceptance_criteria>
    - `gh run list --workflow "Tests" --branch <current> --limit 1 --json conclusion --jq '.[0].conclusion'` returns "success"
    - `.planning/phases/1014-.../1014-VALIDATION.md` updated with `status: approved` + date stamp
    - User-visible signal: human types "approved" / "CI green"
  </acceptance_criteria>
  <resume-signal>Type "approved" (or paste the `gh run list` output showing `MATLAB Tests = success`) to unblock.</resume-signal>
</task>

</tasks>

<verification>
Phase 1014 exit gate:
- All local gates green (MATLAB, Octave, MISS_HIT, phase-level grep).
- CI `MATLAB Tests` job green on push.
- VALIDATION.md marked approved.
- No unauthorized libs/ changes across all 7 plans (only DashboardBuilder.m from Plan 01, plus any Octave-regression follow-up from Task 2 if triggered).
</verification>

<success_criteria>
- 3 Category F residual files fixed.
- Full MATLAB suite runs green.
- Full Octave suite runs green.
- MISS_HIT full sweep clean.
- CI `MATLAB Tests` job returns success on push.
- Phase-level grep gates all return 0.
- VALIDATION.md signed off.
</success_criteria>

<output>
Create `.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-07-SUMMARY.md`:
- Per-file triage table for TestTag / TestToolbar / TestMonitorTagEvents
- Full-suite gate results (MATLAB / Octave / MISS_HIT exit codes)
- Phase-level grep gate results (all three returning 0)
- CI run URL + `gh run view` conclusion
- Final phase-exit commit SHA

Also create `.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-SUMMARY.md` (phase-level rollup):
- Baseline failure count (~140) vs. post-phase (0)
- Total methods DELETED (across all plans)
- Total methods MIGRATED
- Total files deleted (expected: TestEventDetector.m, TestIncrementalDetector.m, possibly TestEventConfig.m, TestLivePipeline.m)
- Library edits authorized: libs/Dashboard/DashboardBuilder.m only (plus any Octave-regression follow-up)
- CI green confirmation
</output>

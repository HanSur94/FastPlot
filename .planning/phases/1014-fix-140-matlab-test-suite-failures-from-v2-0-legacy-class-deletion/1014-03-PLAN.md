---
phase: 1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion
plan: 03
type: execute
wave: 1
depends_on: [1014-01]
files_modified:
  - tests/suite/TestIconCardWidgetTag.m
  - tests/suite/TestMultiStatusWidgetTag.m
  - tests/suite/TestEventDetectorTag.m
  - tests/suite/TestLiveEventPipelineTag.m
  - tests/suite/TestSensorDetailPlotTag.m
  - tests/suite/TestFastSenseAddTag.m
  - tests/suite/TestMonitorTagPersistence.m
autonomous: true
requirements: [PHASE-1014-GATE]
must_haves:
  truths:
    - "No *Tag.m test in this set instantiates Threshold, CompositeThreshold, Sensor, StateChannel, or any deleted legacy class"
    - "Surviving methods exercise the live v2.0 Tag API (SensorTag, MonitorTag, CompositeTag, TagRegistry) and pass on R2020b + Octave"
    - "Per-method triage documented: each deletion vs migration decision recorded in the commit body"
    - "MISS_HIT lint clean on all seven files"
  artifacts:
    - path: "tests/suite/TestIconCardWidgetTag.m"
      provides: "Tag-API IconCard tests with Threshold arms removed"
    - path: "tests/suite/TestMultiStatusWidgetTag.m"
      provides: "Tag-API MultiStatus tests with Threshold arms removed"
    - path: "tests/suite/TestEventDetectorTag.m"
      provides: "EventDetector Tag-overload smoke only; legacy Threshold arms removed"
    - path: "tests/suite/TestLiveEventPipelineTag.m"
      provides: "LiveEventPipeline Tag-only tests; legacy Threshold arms removed"
    - path: "tests/suite/TestSensorDetailPlotTag.m"
      provides: "SensorDetailPlot Tag-only tests; legacy arms removed"
    - path: "tests/suite/TestFastSenseAddTag.m"
      provides: "FastSense.addTag tests; 1 failing method fixed or deleted"
    - path: "tests/suite/TestMonitorTagPersistence.m"
      provides: "MonitorTag persistence tests; 2 erroring methods fixed or deleted"
  key_links:
    - from: "Tag-API tests"
      to: "Live widget/pipeline code paths (SensorTag/MonitorTag branches)"
      via: "Drop-Threshold-arm refactor"
      pattern: "MonitorTag\\(.*ConditionFn"
---

<objective>
Wave 1 `*Tag.m` threshold-call strip. Research finding (RESEARCH.md Pattern 2-bis + Pitfall 3): even the "new" Tag-variant tests still instantiate the deleted `Threshold(...)` class in some methods — confirmed at `TestEventDetectorTag.m:36`, `TestIconCardWidgetTag.m:76`, and others. These `*Tag.m` files were PARTIALLY migrated during Phase 1011; residue remains.

**Strategy (per method):**
1. If the method body needs `Threshold(...)` only as test scaffolding and the assertion actually checks a Tag path (precedence, Tag property, etc.) → **remove the Threshold arm** but keep the method. Example: a method that constructs a widget with both a `Sensor` (deleted) and a `Tag` to verify Tag wins — delete the Sensor/Threshold construction, keep the Tag test.
2. If the method body REQUIRES the deleted class to assert anything meaningful → **delete the method** (same D-02-A heuristic as Plan 02).
3. Never introduce a `MockThreshold` or any shim (D-06).

Purpose: Bring the `*Tag.m` test files to green. These tests exercise the LIVE v2.0 code paths; they are the real acceptance signal for the widget/pipeline layer. Output: 7 files updated, zero legacy-class calls, surviving methods green.

## NOT in scope (deliberately excluded — green in CI)

**`TestFastSenseWidgetTag.m` and `TestEventTimelineWidgetTag.m`** were listed in `1014-RESEARCH.md` §Wave 1 Plan 03 scope but are verified GREEN in CI run 24780979036 with zero legacy-class references. Confirmed locally:

```bash
grep -cE '\b(Threshold|Sensor|ThresholdRule|CompositeThreshold|StateChannel|SensorRegistry)\s*\(' \
    tests/suite/TestFastSenseWidgetTag.m tests/suite/TestEventTimelineWidgetTag.m
# both return 0
```

**Do NOT touch either file in this phase.** RESEARCH.md over-scoped them. The checker will flag them as missing coverage — this is a documented FALSE POSITIVE (see checker run 2, Blockers 1 & 2). Editing green tests is explicitly forbidden by D-06 ("no new test coverage; only restore what compiles").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md
@tests/suite/TestIconCardWidgetTag.m
@tests/suite/TestMultiStatusWidgetTag.m
@tests/suite/TestEventDetectorTag.m
@tests/suite/TestLiveEventPipelineTag.m
@tests/suite/TestSensorDetailPlotTag.m
@tests/suite/TestFastSenseAddTag.m
@tests/suite/TestMonitorTagPersistence.m
@tests/suite/TestCompositeTag.m
@tests/suite/MakePhase1009Fixtures.m
@libs/SensorThreshold/SensorTag.m
@libs/SensorThreshold/MonitorTag.m
@libs/EventDetection/EventDetector.m

<interfaces>
<!-- Live v2.0 contract the surviving tests must hold against -->
SensorTag: `SensorTag('key', 'Name', 'label', 'X', xvec, 'Y', yvec)` — replaces legacy `Sensor(...)`
MonitorTag: `MonitorTag('key', parentSensorTag, @(x,y) y > threshold)` — binary signal, NOT a value object
TagRegistry: `TagRegistry.register(tag)`, `TagRegistry.get(key)`, `TagRegistry.unregister(key)` — hard-errors on duplicate (Pitfall 7)
EventDetector (v2.0, 2-arg only): `det.detect(tag, threshold)` where `threshold` is a MonitorTag-shaped object with a `ConditionFn`
MakePhase1009Fixtures.makeSensorTag — golden SensorTag fixture (tests/suite/MakePhase1009Fixtures.m:23)
MakePhase1009Fixtures.makeMonitorTag — default y > 15 MonitorTag (tests/suite/MakePhase1009Fixtures.m:49)

<!-- Reference: TestCompositeTag.m — the GOLD pattern (already green) -->
From tests/suite/TestCompositeTag.m line ~91:
```matlab
function testAddChildHandle(testCase)
    s = SensorTag('s', 'X', 1:10, 'Y', 1:10);
    m = MonitorTag('m', s, @(x, y) y > 5);
    c = CompositeTag('c', 'and');
    c.addChild(m);
    testCase.verifyEqual(c.getChildCount(), 1);
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip Threshold arms from Tag widget-test files (3 files)</name>
  <files>tests/suite/TestIconCardWidgetTag.m, tests/suite/TestMultiStatusWidgetTag.m, tests/suite/TestSensorDetailPlotTag.m</files>
  <read_first>
    - tests/suite/TestIconCardWidgetTag.m (expected: 3 error methods + 4 verify-failures — RESEARCH says Threshold() on line 76)
    - tests/suite/TestMultiStatusWidgetTag.m (expected: 2 error + 1 verify-fail)
    - tests/suite/TestSensorDetailPlotTag.m (expected: 3 error methods)
    - tests/suite/TestCompositeTag.m (GREEN reference pattern — use TagRegistry.register/unregister + SensorTag + MonitorTag idioms)
    - tests/suite/MakePhase1009Fixtures.m (fixture helpers — reuse makeSensorTag / makeMonitorTag where possible)
    - libs/Dashboard/IconCardWidget.m (confirm which Tag property the widget reads: `Tag` NV or legacy `Threshold` NV)
    - libs/Dashboard/MultiStatusWidget.m (check if `items` struct array still supports mixed Tag/legacy entries — mostly Tag now)
    - libs/FastSense/SensorDetailPlot.m (Tag binding path)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md (D-02-A, D-05)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Pattern 2-bis, Pitfall 1)
  </read_first>
  <action>
**DO NOT TOUCH** `tests/suite/TestFastSenseWidgetTag.m` or `tests/suite/TestEventTimelineWidgetTag.m` — both are green in CI with zero legacy refs (see "NOT in scope" in <objective>). Editing them violates D-06.

For each of the three in-scope files, apply per-method triage:

**For each `function testX(testCase)` method:**

1. grep the body for `Threshold(` / `CompositeThreshold(` / `Sensor(` / `ThresholdRule(` / `StateChannel(` / `detectEventsFromSensor(`.
2. If NO hit: method is already clean; leave untouched.
3. If hits exist, analyze the assertion (`verifyEqual`, `verifyTrue`, etc.):
   - (a) Does the assertion touch a legacy field (`.Sensor.Thresholds`, `.IsUpper`, `.allValues()`)? → **DELETE** the method.
   - (b) Does the assertion touch a Tag-API field (`.Tag`, `.MonitorTag`, `TagRegistry.get`, Tag-carrier `.Keys`)? → **STRIP** the Threshold arm: delete the `Threshold(...)` construction line(s) and any subsequent code that only existed to feed it; keep the Tag-path code and the assertion.
   - (c) Ambiguous? Default to DELETE (D-02-A default).

**Specific known cases from RESEARCH:**

- `TestIconCardWidgetTag.m` line ~76: `t = Threshold(...)` then widget constructed with BOTH `Threshold` and `Tag`. The test's intent was "Tag precedence" — keep the Tag construction, delete the Threshold line, and simplify the assertion to "the widget bound to Tag renders per Tag". If that assertion isn't meaningful without the Threshold contrast, DELETE the method.

- `TestMultiStatusWidgetTag.m`: `items` struct array mixed Tag + Threshold entries. Keep only Tag entries; drop Threshold-typed entries; simplify assertion.

- `TestSensorDetailPlotTag.m`: likely legacy `Sensor(...)` construction feeding into SensorDetailPlot. Replace with `SensorTag('key', 'X', ..., 'Y', ...)` IF the widget's Tag path is well-defined; otherwise DELETE.

**Fixture reuse:** Where you need a golden Tag, prefer `MakePhase1009Fixtures.makeSensorTag()` and `.makeMonitorTag()` over inline construction — keeps test style consistent.

**Registry hygiene:** If a kept method registers a Tag into `TagRegistry`, add a teardown to `TagRegistry.unregister(key)`. Pattern from TestCompositeTag.m:
```matlab
testCase.addTeardown(@() tryUnregister('my_key'));

function tryUnregister(key)
    try, TagRegistry.unregister(key); catch, end
end
```

**Empty-block guard (Pitfall 5):** after deletions, ensure `methods (Test)` contains >= 1 method. Add a constructor smoke test if needed.

**Commit discipline (D-04):** one commit per file:
  - `fix(1014-test): strip Threshold arms from TestIconCardWidgetTag`
  - `fix(1014-test): strip Threshold arms from TestMultiStatusWidgetTag`
  - `fix(1014-test): strip Threshold arms from TestSensorDetailPlotTag`

Each commit body lists: methods DELETED, methods MIGRATED (Threshold arm stripped), methods UNTOUCHED.

**D-05 kill-switch:** if any single file > 45 min → switch to pure deletion for all remaining ambiguous methods in that file.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); for T = {'TestIconCardWidgetTag','TestMultiStatusWidgetTag','TestSensorDetailPlotTag'}; runtests(['tests/suite/' T{1} '.m']); end" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 3 files: `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/<file>` returns 0
    - `grep -c "detectEventsFromSensor" tests/suite/TestIconCardWidgetTag.m tests/suite/TestMultiStatusWidgetTag.m tests/suite/TestSensorDetailPlotTag.m` returns 0
    - `git diff --name-only -- tests/suite/TestFastSenseWidgetTag.m tests/suite/TestEventTimelineWidgetTag.m` returns empty (NOT-in-scope files untouched)
    - `matlab -batch "runtests('tests/suite/TestIconCardWidgetTag.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestMultiStatusWidgetTag.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestSensorDetailPlotTag.m')" ; echo $?` returns 0
    - `mh_style` + `mh_lint` on the three files exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - 3 commits landed with `fix(1014-test): strip Threshold arms from ...` prefix
  </acceptance_criteria>
  <done>
Three Tag widget-test files clean of legacy-class references, all surviving methods pass, MISS_HIT clean, 3 commits landed. TestFastSenseWidgetTag.m and TestEventTimelineWidgetTag.m NOT touched (green-in-CI guard).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Strip Threshold arms from Tag event/pipeline test files (2 files) + FastSense Tag test</name>
  <files>tests/suite/TestEventDetectorTag.m, tests/suite/TestLiveEventPipelineTag.m, tests/suite/TestFastSenseAddTag.m</files>
  <read_first>
    - tests/suite/TestEventDetectorTag.m (RESEARCH: at least 3 methods with `Threshold(` at line 36)
    - tests/suite/TestLiveEventPipelineTag.m (4 error + 2 verify-fail per CI log)
    - tests/suite/TestFastSenseAddTag.m (1 error per CI log)
    - libs/EventDetection/EventDetector.m (current 2-arg signature: `det.detect(tag, threshold)` where threshold has `.ConditionFn`)
    - libs/EventDetection/LiveEventPipeline.m (v2.0 takes `MonitorTargets` directly)
    - libs/FastSense/FastSense.m (addTag method — dispatches by `tag.getKind()`)
    - tests/suite/MakePhase1009Fixtures.m
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Pattern 2-bis + Pitfall 2)
  </read_first>
  <action>
Apply the same per-method triage as Task 1 to these three files. Specific cases:

**TestEventDetectorTag.m** — The 2-arg `det.detect(tag, threshold)` path is live but `threshold` cannot be a deleted `Threshold` instance. For each method:
  - If `Threshold(...)` is used to construct the 2nd argument → DELETE the method (there is no v2.0 substitute for a value-object threshold; use `MonitorTag` with `ConditionFn` IF the assertion makes sense with a boolean signal, otherwise delete).
  - If the method uses only `MonitorTag` or `SensorTag` → leave untouched.
  - Keep at minimum one smoke test that constructs `EventDetector()` with no detect() call, e.g., `testConstructorSmoke` (Pitfall 5 guard).

**TestLiveEventPipelineTag.m** — `LiveEventPipeline` takes `MonitorTargets` directly post-Phase 1009 (STATE.md line 230). Any method using the old `addSensor` / `Threshold` wiring → DELETE. Methods using `MonitorTargets` map → keep, strip any Threshold arms.

**TestFastSenseAddTag.m** — Only 1 error per CI log. Diagnose the exact failing method:
  - grep for deleted-class calls. Most likely it is a leftover `Sensor(...)` construction fed to `addTag` — update to `SensorTag(...)` using the correct v2.0 constructor (`SensorTag('key', 'X', xvec, 'Y', yvec)`).
  - If the method was asserting a legacy-only branch in FastSense.addTag, DELETE (per STATE.md line 201: FastSense.addTag dispatches on tag.getKind() — there are no legacy `isa(..., 'Sensor')` branches).

**Commit discipline (D-04):** one commit per file:
  - `fix(1014-test): strip Threshold arms from TestEventDetectorTag`
  - `fix(1014-test): strip Threshold arms from TestLiveEventPipelineTag`
  - `fix(1014-test): fix TestFastSenseAddTag Sensor→SensorTag migration`
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); for T = {'TestEventDetectorTag','TestLiveEventPipelineTag','TestFastSenseAddTag'}; runtests(['tests/suite/' T{1} '.m']); end" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 3 files: `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/<file>` returns 0
    - `grep -c "detectEventsFromSensor" tests/suite/TestEventDetectorTag.m tests/suite/TestLiveEventPipelineTag.m tests/suite/TestFastSenseAddTag.m` returns 0
    - `matlab -batch "runtests('tests/suite/TestEventDetectorTag.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestLiveEventPipelineTag.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestFastSenseAddTag.m')" ; echo $?` returns 0
    - `mh_style` + `mh_lint` on the three files exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - 3 commits landed
  </acceptance_criteria>
  <done>
Three event/pipeline/FastSense Tag test files clean of legacy-class references, surviving methods pass, MISS_HIT clean, 3 commits landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix TestMonitorTagPersistence errors</name>
  <files>tests/suite/TestMonitorTagPersistence.m</files>
  <read_first>
    - tests/suite/TestMonitorTagPersistence.m (2 error methods + 3 verify-fail methods — 5 failing total)
    - libs/SensorThreshold/MonitorTag.m (current Persist/DataStore contract; see STATE.md line 217 — Phase 1007 persist pairing + staleness)
    - libs/FastSense/FastSenseDataStore.m (ensureMonitorsTable_ helper; storeMonitor API)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Category F triage table entries for this file)
  </read_first>
  <action>
Per-method triage of the 5 failing methods:

STEP 1 — grep the file for deleted-class references:
```
grep -nE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/TestMonitorTagPersistence.m
```
Any method hit → apply Task 1-style triage (strip arm or delete).

STEP 2 — For the verify-fail methods (e.g., `testMonitorTagHasPersistProperties`, `testFastSenseDataStoreHasMonitorAPI`, `testPitfall2StructuralGate` — all regex-over-source gates per RESEARCH table):
  - Update the regex/expected-text patterns to match the CURRENT `MonitorTag.m` / `FastSenseDataStore.m` source.
  - Widen regex windows if Phase 1010 added lines (e.g., `testPitfall2StructuralGate` — RESEARCH says `EventBinding.attach` calls in the 5-line window around `storeMonitor` broke the gate; widen the window to accommodate).
  - These are test-only fixes — do NOT edit `libs/`.

STEP 3 — If any method's asserted contract has genuinely changed (e.g., persistence now requires an env var that didn't exist pre-1007), update the test's setup to match current behaviour. Reference STATE.md line 217 for Phase 1007 decisions.

STEP 4 — Commit: `fix(1014-test): update TestMonitorTagPersistence regex gates + clean legacy refs`

STEP 5 — Pitfall 5 guard: ensure the file still compiles after any deletions.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); runtests('tests/suite/TestMonitorTagPersistence.m')" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/TestMonitorTagPersistence.m` returns 0
    - `matlab -batch "runtests('tests/suite/TestMonitorTagPersistence.m')" ; echo $?` returns 0
    - `mh_style tests/suite/TestMonitorTagPersistence.m && mh_lint tests/suite/TestMonitorTagPersistence.m` exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - Commit `fix(1014-test): update TestMonitorTagPersistence regex gates + clean legacy refs` landed
  </acceptance_criteria>
  <done>
TestMonitorTagPersistence.m passes all methods, zero legacy-class refs, MISS_HIT clean, commit landed.
  </done>
</task>

</tasks>

<verification>
- 7 `*Tag.m` / Tag-adjacent files clean of legacy-class constructor calls.
- All surviving methods pass on MATLAB + Octave.
- Commit graph: 7 commits (one per file per D-04).
- MISS_HIT clean.
- `TestFastSenseWidgetTag.m` and `TestEventTimelineWidgetTag.m` NOT touched (green-in-CI exclusion).
- Plans 02, 04, 05, 06 can run in parallel — no file overlap.
</verification>

<success_criteria>
- 7 files updated.
- ~20-30 erroring methods now pass or are gone.
- 7 atomic commits landed.
- MISS_HIT clean.
- SUMMARY.md documents per-file: methods deleted, methods migrated (Threshold arm stripped), methods untouched.
- SUMMARY.md confirms TestFastSenseWidgetTag.m / TestEventTimelineWidgetTag.m not modified.
</success_criteria>

<output>
Create `.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-03-SUMMARY.md` with per-file triage breakdown (deleted vs. stripped vs. unchanged), Octave-green confirmation, and explicit "NOT touched" line for the two green-in-CI files.
</output>
</content>
</invoke>
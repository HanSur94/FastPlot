---
phase: 1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion
plan: 05
type: execute
wave: 1
depends_on: [1014-01]
files_modified:
  - tests/suite/TestEventDetector.m
  - tests/suite/TestIncrementalDetector.m
  - tests/suite/TestEventStore.m
  - tests/suite/TestEventConfig.m
  - tests/suite/TestLivePipeline.m
autonomous: true
requirements: [PHASE-1014-GATE]
must_haves:
  truths:
    - "TestEventDetector.m and TestIncrementalDetector.m are DELETED entirely (all methods call deleted 6-arg pipelines)"
    - "TestEventStore.m, TestEventConfig.m, TestLivePipeline.m have zero legacy-class constructor calls"
    - "run_all_tests.m still discovers surviving test classes without error"
    - "EventStoreRw.m coverage (pure-EventStore persistence) is untouched and still green"
    - "MISS_HIT clean on all edited files"
  artifacts:
    - path: "tests/suite/TestEventStore.m"
      provides: "EventStore tests with legacy pipelines stripped"
    - path: "tests/suite/TestEventConfig.m"
      provides: "EventConfig tests with addSensor/runDetection methods stripped"
    - path: "tests/suite/TestLivePipeline.m"
      provides: "LivePipeline tests with Sensor.resolve pipeline methods stripped"
  key_links:
    - from: "Surviving test methods"
      to: "Live EventStore + MonitorTag event path"
      via: "MonitorTag.appendData + EventStore.append"
      pattern: "(EventStore\\.append|MonitorTag\\.appendData)"
---

<objective>
Wave 1 EventDetection test-suite collapse. Research: the legacy Sensor-centric event detection pipeline is GONE. `EventDetector.detect` has only the 2-arg overload; `IncrementalEventDetector.process` is a stub that throws `legacyRemoved`; `EventConfig.addSensor`/`.runDetection` return empty or are deleted; `LiveEventPipeline` rewired to `MonitorTargets` directly in Phase 1009.

**Strategy:**
- DELETE entirely: `TestEventDetector.m` (all 8 methods call deleted 6-arg form) and `TestIncrementalDetector.m` (all 8 methods call deleted `process(6-arg)`).
- TRIAGE per-method: `TestEventStore.m` (7 failures), `TestEventConfig.m` (6 failures), `TestLivePipeline.m` (8 failures) — delete methods using deleted APIs; keep pure-live methods.

Per Pitfall 5: when deleting whole files, delete the `.m` file (do NOT leave an empty classdef). `TestSuite.fromFolder` auto-discovers, so no index update needed.

Per RESEARCH.md: "live EventStore persistence is already covered by `TestEventStoreRw.m`" — DO NOT touch TestEventStoreRw. It's the live-path coverage.

Purpose: Close the 37-method cliff in EventDetection tests. Output: 2 files deleted, 3 files pruned, zero legacy-class refs, surviving methods green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md
@tests/suite/TestEventDetector.m
@tests/suite/TestIncrementalDetector.m
@tests/suite/TestEventStore.m
@tests/suite/TestEventStoreRw.m
@tests/suite/TestEventConfig.m
@tests/suite/TestLivePipeline.m
@tests/suite/TestLiveEventPipelineTag.m
@libs/EventDetection/EventDetector.m
@libs/EventDetection/IncrementalEventDetector.m
@libs/EventDetection/EventConfig.m
@libs/EventDetection/EventStore.m
@libs/EventDetection/LiveEventPipeline.m

<interfaces>
<!-- v2.0 live APIs — surviving tests may use these -->
EventStore:     store.append(event), store.load(path), store.save(path) — live path
MonitorTag:     m.appendData(newX, newY) — fires events via OnEventStart/OnEventEnd or EventStore wiring
EventDetector:  det.detect(tag, threshold) — 2-arg; threshold is MonitorTag-shaped (ConditionFn)
LiveEventPipeline: takes `MonitorTargets` map directly (Phase 1009)

<!-- Deleted / stub APIs — tests using these must be pruned -->
EventDetector.detect(t, values, val, dir, label, name)         — DELETED (6-arg form never existed in v2.0)
IncrementalEventDetector.process(key, sensor, t, y, ...)       — STUB that throws 'legacyRemoved'
EventConfig.addSensor(s)                                       — DELETED
EventConfig.runDetection()                                     — returns empty (stub)
EventConfig.addTag(...)                                        — does NOT exist
detectEventsFromSensor(...)                                    — standalone function, DELETED
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete TestEventDetector.m and TestIncrementalDetector.m entirely</name>
  <files>tests/suite/TestEventDetector.m, tests/suite/TestIncrementalDetector.m</files>
  <read_first>
    - tests/suite/TestEventDetector.m (confirm all 8 methods call deleted 6-arg form)
    - tests/suite/TestIncrementalDetector.m (confirm all 8 methods call `process(6-arg)` which hits `legacyRemoved`)
    - tests/suite/TestEventDetectorTag.m (DO NOT edit — Plan 03 owns it; confirm it exists as the live-path replacement)
    - libs/EventDetection/EventDetector.m lines 1-80 (confirm 2-arg-only signature)
    - libs/EventDetection/IncrementalEventDetector.m (confirm process() is a `legacyRemoved` stub)
    - tests/run_all_tests.m (confirm auto-discovery — no index to update)
    - scripts/run_tests_with_coverage.m (confirm MATLAB suite discovery — no index to update)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md (D-06 — deleting fully-obsolete file is in-scope; only RENAMING is forbidden)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Pitfall 2, Code Examples Category B)
  </read_first>
  <action>
STEP 1 — Verify obsolescence before deletion. For each file:
```
grep -cE "det\.detect\s*\(" tests/suite/TestEventDetector.m         # expect > 0 on every test method
grep -cE "\.process\s*\(" tests/suite/TestIncrementalDetector.m     # expect > 0 on every test method
```
If there is ANY method that does NOT call the deleted 6-arg form AND tests a live v2.0 behaviour, DO NOT bulk-delete the file — promote that method to `TestEventDetectorTag.m` instead (check with Plan 03 first). Research's expectation is 100% deletion; verify.

STEP 2 — Delete the files:
```
git rm tests/suite/TestEventDetector.m
git rm tests/suite/TestIncrementalDetector.m
```

STEP 3 — Confirm auto-discovery still works:
```
matlab -batch "addpath('scripts'); run_tests_with_coverage()"   # full suite, expect no "missing TestEventDetector" error
octave --eval "cd tests; run_all_tests()"                       # function-style suite, should be unaffected
```

STEP 4 — Commit EACH file deletion separately (D-04):
  - `fix(1014-test): delete TestEventDetector.m — all methods call deleted 6-arg detect()`
  - `fix(1014-test): delete TestIncrementalDetector.m — all methods call legacyRemoved process() stub`

Each commit body cites: exact method names (for git-log bisectability), research finding ("RESEARCH.md Pattern 2 / Pitfall 2: entire legacy pipeline deleted").
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); run_tests_with_coverage()" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/suite/TestEventDetector.m 2>/dev/null` returns nothing (file gone)
    - `ls tests/suite/TestIncrementalDetector.m 2>/dev/null` returns nothing (file gone)
    - `ls tests/suite/TestEventDetectorTag.m` confirms replacement file still exists (Plan 03's scope)
    - `matlab -batch "run_tests_with_coverage()" ; echo $?` runs — auto-discovery works, no error about missing files
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - 2 commits landed, one per file deletion
  </acceptance_criteria>
  <done>
Two files deleted, 2 commits landed. Test suite auto-discovery still works. Octave green. Live-path coverage in `TestEventDetectorTag.m` (Plan 03 scope) remains.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prune TestEventStore.m, TestEventConfig.m, TestLivePipeline.m (per-method triage)</name>
  <files>tests/suite/TestEventStore.m, tests/suite/TestEventConfig.m, tests/suite/TestLivePipeline.m</files>
  <read_first>
    - tests/suite/TestEventStore.m (7 error methods per CI log; research says most call `cfg.addTag` + `runDetection` — DELETE them; keep `testFromFileNotFound` and pure-persistence tests)
    - tests/suite/TestEventStoreRw.m (read-only — confirms live-path coverage exists; DO NOT touch)
    - tests/suite/TestEventConfig.m (6 error methods per CI log; likely all call `addSensor` or similar)
    - tests/suite/TestLivePipeline.m (8 error methods per CI log — RESEARCH body not read; planner assumed all-delete)
    - tests/suite/TestLiveEventPipelineTag.m (DO NOT touch — Plan 03 scope; this is the v2.0 replacement)
    - libs/EventDetection/EventConfig.m (confirm addSensor is deleted; addTag does not exist; runDetection is a stub)
    - libs/EventDetection/LiveEventPipeline.m (confirm MonitorTargets is the v2.0 wiring)
    - libs/EventDetection/EventStore.m (live API: append, save, load, fromFile)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Pitfall 3 — "feeling guilty about deletion")
  </read_first>
  <action>
Apply per-method triage to EACH file. Process files in order: TestEventStore.m → TestEventConfig.m → TestLivePipeline.m.

**General rule (same as Plan 02):**
- Method body uses `cfg.addSensor`, `cfg.addTag`, `cfg.runDetection`, `detectEventsFromSensor`, `Sensor(...)`, `Threshold(...)`, `ThresholdRule(...)`, `IncrementalEventDetector.process`, 6-arg `det.detect(...)`, or any deleted helper → **DELETE** the method.
- Method body uses only live v2.0 APIs (`EventStore.append/save/load/fromFile`, direct `Event(...)` struct construction that still works, pure-file I/O with no legacy wiring) → **KEEP**.

**TestEventStore.m — specific triage:**
- DELETE methods that call `cfg.addTag(s)` followed by `cfg.runDetection()` — those are the 7 failing methods per RESEARCH.
- KEEP `testFromFileNotFound` (pure static, no legacy dep — confirm by reading its body).
- KEEP any method that does `EventStore.append(evt)` + `save` + `load` round-trip with no `EventConfig` / `EventDetector` wiring.

**TestEventConfig.m — specific triage:**
- EventConfig's `addSensor` is deleted; `addTag` does not exist; `runDetection` stub returns empty. Any method calling these: DELETE.
- Trivial property-getter tests (e.g., `testConfigHasNameField`) may survive — keep them.
- If literally all 6 failing methods use the deleted API AND no green methods remain, consider deleting the whole file (Pitfall 5 guard). Check with a smoke test first:
  ```
  matlab -batch "runtests('tests/suite/TestEventConfig.m')" ; echo $?
  ```
  If some methods pass (not listed in CI failures), keep them. If none pass, delete the file:
  ```
  git rm tests/suite/TestEventConfig.m
  ```

**TestLivePipeline.m — specific triage:**
- RESEARCH says body unread; plan for "mostly-delete". grep body:
  ```
  grep -cE "addSensor|addTag|\.process\s*\(|Threshold\s*\(|Sensor\s*\(" tests/suite/TestLivePipeline.m
  ```
- If every method hits that grep: delete the whole file. Live-path coverage lives in `TestLiveEventPipelineTag.m` (Plan 03 scope).
- If some methods are pure (e.g., construct LiveEventPipeline with MonitorTargets directly, no legacy wiring): KEEP those; delete the rest.

**Pitfall 5 guard (all three files):** after deletions, confirm `methods (Test)` block is non-empty. If it would be empty, delete the entire `.m` file instead.

**Commit discipline (D-04):** one commit per file (or per deletion):
  - `fix(1014-test): prune TestEventStore — delete legacy addSensor/runDetection methods`
  - `fix(1014-test): prune TestEventConfig — delete legacy addSensor/addTag methods` (or `delete TestEventConfig.m` if whole-file)
  - `fix(1014-test): prune TestLivePipeline — delete legacy pipeline methods` (or `delete TestLivePipeline.m` if whole-file)

Each commit body lists methods deleted vs. kept.

**D-05 kill-switch:** if any single file > 30 min (lower threshold here given the high-deletion ratio), switch to whole-file deletion. The live-path coverage is in `TestEventStoreRw.m` and `TestLiveEventPipelineTag.m`.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); for T = {'TestEventStore','TestEventConfig','TestLivePipeline'}; f=['tests/suite/' T{1} '.m']; if exist(f,'file'), runtests(f); end; end" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - For each surviving file: `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/<file>` returns 0
    - `grep -c "detectEventsFromSensor" tests/suite/TestEventStore.m tests/suite/TestEventConfig.m tests/suite/TestLivePipeline.m 2>/dev/null` returns 0 (treating missing files as 0)
    - `grep -cE "\.addSensor\s*\(|\.runDetection\s*\(" tests/suite/TestEventConfig.m 2>/dev/null` returns 0 (treating missing file as 0)
    - Each surviving file compiles: `matlab -batch "clear classes; for T = {'TestEventStore','TestEventConfig','TestLivePipeline'}; f=['tests/suite/' T{1} '.m']; if exist(f,'file'), addpath('tests/suite'); eval(T{1}); end; end; disp('ok')"` exits 0
    - `matlab -batch "run_tests_with_coverage()" ; echo $?` returns 0 or, if other plans haven't landed yet, returns with monotonic reduction in failure count (no new errors introduced by this plan)
    - `ls tests/suite/TestEventStoreRw.m` confirms the live-path coverage file still exists and was NOT touched
    - `ls tests/suite/TestLiveEventPipelineTag.m` confirms Plan 03's file still exists and was NOT touched
    - `mh_style` + `mh_lint` on any surviving file exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - 3 commits landed (or 2-3 commits if any whole-file deletion combined in one commit)
  </acceptance_criteria>
  <done>
Three files pruned (or deleted). Zero legacy-class constructor calls. Surviving methods pass. MISS_HIT clean. Octave green. Live-path coverage in `TestEventStoreRw.m` and `TestLiveEventPipelineTag.m` intact.
  </done>
</task>

</tasks>

<verification>
- 2 files deleted (TestEventDetector, TestIncrementalDetector).
- 3 files pruned or deleted (TestEventStore, TestEventConfig, TestLivePipeline).
- Zero legacy-class refs in anything surviving.
- Octave suite green.
- TestEventStoreRw.m and TestLiveEventPipelineTag.m untouched.
- MISS_HIT clean.
</verification>

<success_criteria>
- ~37 erroring EventDetection methods are gone (deleted) or fixed.
- 2-3 whole-file deletions plus per-method pruning in 0-3 survivors.
- 4-5 atomic commits landed.
- MISS_HIT clean.
- SUMMARY documents file-by-file: DELETED vs. PRUNED status.
</success_criteria>

<output>
Create `.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-05-SUMMARY.md`:
- For each of the 5 target files: final status (file deleted / methods pruned / unchanged) + exact method names affected
- Confirmation TestEventStoreRw.m and TestLiveEventPipelineTag.m were not touched
- Total method count eliminated (target ~37)
- D-05 outcome (invoked or not)
</output>

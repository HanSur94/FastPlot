---
phase: 1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion
plan: 02
type: execute
wave: 1
depends_on: [1014-01]
files_modified:
  - tests/suite/TestStatusWidget.m
  - tests/suite/TestGaugeWidget.m
  - tests/suite/TestIconCardWidget.m
  - tests/suite/TestMultiStatusWidget.m
  - tests/suite/TestChipBarWidget.m
autonomous: true
requirements: [PHASE-1014-GATE]
must_haves:
  truths:
    - "No test in these 5 widget-test files instantiates Threshold, CompositeThreshold, Sensor, StateChannel, SensorRegistry, ThresholdRule, ThresholdRegistry, or ExternalSensorRegistry"
    - "No test calls Sensor.addThreshold or detectEventsFromSensor"
    - "Surviving methods (render, constructor, fromStruct, basic getType) pass on MATLAB R2020b and Octave"
    - "Parallel Tag-API coverage in *Tag.m files still exists (untouched by this plan; Plan 03 handles *Tag.m threshold strip)"
    - "MISS_HIT lint clean on all five files after deletion"
  artifacts:
    - path: "tests/suite/TestStatusWidget.m"
      provides: "Status widget tests minus deleted-threshold methods"
      contains: "classdef TestStatusWidget"
    - path: "tests/suite/TestGaugeWidget.m"
      provides: "Gauge widget tests minus deleted-threshold methods"
      contains: "classdef TestGaugeWidget"
    - path: "tests/suite/TestIconCardWidget.m"
      provides: "IconCard widget tests minus deleted-threshold methods"
      contains: "classdef TestIconCardWidget"
    - path: "tests/suite/TestMultiStatusWidget.m"
      provides: "MultiStatus widget tests minus deleted-threshold/composite methods"
      contains: "classdef TestMultiStatusWidget"
    - path: "tests/suite/TestChipBarWidget.m"
      provides: "ChipBar widget tests minus deleted-threshold methods"
      contains: "classdef TestChipBarWidget"
  key_links:
    - from: "Deleted test methods"
      to: "Deleted widget threshold branches"
      via: "D-02-A heuristic — no live code path to exercise"
      pattern: "N/A (deletion link)"
---

<objective>
Wave 1 widget-threshold DELETE batch. Five widget test classes instantiate the deleted `Threshold`, `CompositeThreshold`, `Sensor`, etc. classes in order to exercise widget branches that are themselves dead code (confirmed by RESEARCH.md: `StatusWidget.m:162-200` and siblings still read `t.IsUpper`/`t.allValues()` but no object can be passed through that branch post-Phase 1011).

**Strategy:** DELETE every test method whose body instantiates a deleted class. Keep methods that exercise live v2.0 paths (constructor with defaults, plain `render()`, `getType()`, `fromStruct` with no legacy objects, sensor-based paths using `SensorTag`, `Tag` property tests).

Per D-02-A: "If the test was specifically exercising deleted behaviour [...] and no v2.0 equivalent exists → delete the individual `function testX` method." Parallel coverage for the live Tag-API path lives in the `*Tag.m` test files (Plan 03's scope).

Per D-05 kill-switch: if any single file takes > 45 minutes, DELETE methods rather than migrate. This plan defaults to delete; migration is the rare exception.

Purpose: Remove dead-code-test coupling so the suite compiles and the surviving widget tests run green. Output: 5 slimmer test files, zero legacy-class constructor calls, zero errored tests in these files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md
@tests/suite/TestStatusWidget.m
@tests/suite/TestGaugeWidget.m
@tests/suite/TestIconCardWidget.m
@tests/suite/TestMultiStatusWidget.m
@tests/suite/TestChipBarWidget.m
@tests/suite/TestCompositeTag.m
@libs/Dashboard/StatusWidget.m
@libs/Dashboard/GaugeWidget.m

<interfaces>
<!-- Deleted classes — ANY construction of these is the deletion signal -->
- Threshold(...)              → class file removed in commit 4188a7f
- CompositeThreshold(...)     → class file removed in commit 4188a7f
- Sensor(...)                 → class file removed in commit 4188a7f
- StateChannel(...)           → class file removed in commit 4188a7f
- SensorRegistry.register/get → class file removed in commit 4188a7f
- ThresholdRule(...)          → class file removed in commit 4188a7f
- ThresholdRegistry(...)      → class file removed in commit 4188a7f
- ExternalSensorRegistry(...) → class file removed in commit 4188a7f
- Sensor.addThreshold(...)    → method removed (along with Sensor class)
- detectEventsFromSensor(...) → standalone function removed

<!-- LIVE v2.0 APIs that surviving tests MAY use -->
- SensorTag, StateTag, MonitorTag, CompositeTag, TagRegistry
- MonitorTag('key', parentSensorTag, @(x,y) y > threshold)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete legacy-class methods from TestStatusWidget.m and TestGaugeWidget.m</name>
  <files>tests/suite/TestStatusWidget.m, tests/suite/TestGaugeWidget.m</files>
  <read_first>
    - tests/suite/TestStatusWidget.m (enumerate every `function testX` method; note which ones call Threshold/Sensor/ThresholdRule/etc.)
    - tests/suite/TestGaugeWidget.m (same enumeration)
    - libs/Dashboard/StatusWidget.m (confirm the `obj.Sensor.Thresholds` / `IsUpper` / `allValues()` branches exist and are reachable only from deleted-class instances)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md (D-02-A heuristic, D-04 commit discipline, D-05 kill-switch)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Pattern 1, Pitfall 1, Pitfall 5 — don't leave empty `methods (Test)` block)
  </read_first>
  <action>
**Enumerated deletion list (from grep run 2026-04-22 — executor uses this list; grep rule below is defense-in-depth).**

**TestStatusWidget.m — DELETE these 10 methods:**
  - testDeriveStatusFromSensorWithThresholds (line 109) — uses Threshold() + .addThreshold()
  - testConstructorThresholdBinding (line 227) — uses Threshold()
  - testThresholdKeyResolution (line 238) — uses Threshold()
  - testMutualExclusivity (line 251) — uses Threshold()
  - testDeriveStatusFromThreshold (line 265) — uses Threshold()
  - testThresholdPathPriority (line 292) — uses Threshold()
  - testValueFcnLiveTick (line 308) — uses Threshold()
  - testSerializeThresholdRoundTrip (line 332) — uses Threshold()
  - testThresholdValueLabel (line 359) — uses Threshold()
  - testLowerThresholdViolation (line 379) — uses Threshold()
  (KEEP: testConstruction, testDefaultPosition, testRender, testRefreshStaticStatus, testRefreshWithStatusFcn, testRefreshWithTag, testToStruct, testToStructWithStaticStatus, testFromStruct, testGetType — 10 survivors)

**TestGaugeWidget.m — DELETE these 7 methods:**
  - testRangeDeriveFromTag (line 136) — uses Threshold() + .addThreshold()
  - testConstructorThresholdBinding (line 208) — uses Threshold()
  - testThresholdRangeDerivation (line 219) — uses Threshold()
  - testThresholdColorPath (line 229) — uses Threshold()
  - testMutualExclusivity (line 253) — uses Threshold()
  - testSerializeThresholdRoundTrip (line 267) — uses Threshold()
  - testThresholdWithValueFcn (line 292) — uses Threshold()
  (KEEP: testConstruction, testDefaultPosition, testFourStyles, testRenderArc, testRenderDonut, testRenderBar, testRenderThermometer, testRefreshStaticValue, testRefreshWithValueFcn, testRefreshWithTag, testUnitsDeriveFromTag, testToStruct, testFromStruct, testGetType — 14 survivors)

Every method named above traces to a CI-log error in `1014-CONTEXT.md §specifics` (TestStatusWidget: 11 error methods + 1 verify-fail; TestGaugeWidget: 7 error + 1 verify-fail — the verify-fails are subsumed into the method list above where they share names, or are the pure-render methods that should start passing once legacy methods are gone and Plan 01 TestData migration has landed).

**DEFENSE-IN-DEPTH grep rule (catches anything the enumeration missed):**

For EACH of the two files (TestStatusWidget.m then TestGaugeWidget.m):

STEP 1 — Enumerate methods: run `grep -nE "^\s*function\s+\w+\s*\(testCase" <file>` to list every test method with line numbers. Cross-check against the enumerated list above — if any method matching the deletion pattern is NOT in the list above, delete it too and note the discrepancy in the commit body.

STEP 2 — For each `function testX(testCase)` method, apply the deletion rule:

DELETE the method (entire `function ... end` block including its header comment if any) if its body contains ANY of:
  - `Threshold(` (constructor call — word-boundary matches 'Threshold' but not 'MonitorTag'/'ThresholdLabel' etc.)
  - `CompositeThreshold(`
  - `Sensor(` (word-boundary; do not match `SensorTag(` or `MockSensor(`)
  - `ThresholdRule(`
  - `StateChannel(`
  - `SensorRegistry.` or `ThresholdRegistry.` or `ExternalSensorRegistry.`
  - `.addThreshold(` where the receiver is the result of `Sensor(...)` or similar deleted-class instance
  - `detectEventsFromSensor(`

KEEP the method if it tests:
  - Constructor with defaults (no deleted-class args): e.g., `testConstructorDefaults`, `testConstructorSetsKey`
  - `render()` / `refresh()` with no threshold or sensor binding
  - `getType()` returning the right string
  - `toStruct()` / `fromStruct()` round-trip where the struct does not reference a deleted class
  - Behaviour using `SensorTag`, `MonitorTag`, `Tag` property — these are live v2.0 APIs (though these cases are rare in non-`*Tag` files; most such coverage lives in `*Tag.m`).

STEP 3 — After deleting methods, verify the `methods (Test)` block still contains at least ONE method (Pitfall 5). If the block would be empty:
  - Do NOT leave an empty `methods (Test)` block — that is a MATLAB syntax error.
  - If at least one non-legacy method remains, you're fine.
  - If literally every test uses a deleted class: keep one minimal smoke test. Rewrite it as:
    ```matlab
    function testConstructorSmoke(testCase)
        w = StatusWidget('key1', 'Position', [1 1 1 1]);
        testCase.verifyEqual(w.getType(), 'status');
    end
    ```
  - (Use the correct class name per file: `StatusWidget`, `GaugeWidget`.)

STEP 4 — Remove any now-unused helper methods in `methods (Access = private)` that were only called from deleted test methods (grep for references; if zero, delete the helper).

STEP 5 — For each deleted method, leave a one-line comment-trail in the commit message listing the method name. DO NOT leave dead method stubs or `%TODO re-add when Tag migration done` comments in the file — CONTEXT.md D-06 forbids "coverage gap audits."

STEP 6 — Commit EACH FILE separately (D-04):
  - `fix(1014-test): delete legacy-class methods from TestStatusWidget`
  - `fix(1014-test): delete legacy-class methods from TestGaugeWidget`
  - In each commit body, list the exact method names deleted.

STEP 7 — D-05 kill-switch: if for some reason you are tempted to rewrite a method instead of deleting it, CONFIRM the receiver widget code can actually respond to the new Tag object (grep StatusWidget.m for `MonitorTag`). If it only reads `Threshold.IsUpper`, the widget path is dead; migration is pointless. Delete.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); runtests('tests/suite/TestStatusWidget.m'); runtests('tests/suite/TestGaugeWidget.m')" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/TestStatusWidget.m` returns 0 (note: matches the bare word, not SensorTag / MonitorTag)
    - `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/TestGaugeWidget.m` returns 0
    - `grep -c "detectEventsFromSensor" tests/suite/TestStatusWidget.m tests/suite/TestGaugeWidget.m` returns 0
    - `matlab -batch "runtests('tests/suite/TestStatusWidget.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestGaugeWidget.m')" ; echo $?` returns 0
    - Each file still compiles: `matlab -batch "clear classes; TestStatusWidget; TestGaugeWidget; disp('ok')"` exits 0 (class loads)
    - `mh_style tests/suite/TestStatusWidget.m tests/suite/TestGaugeWidget.m && mh_lint tests/suite/TestStatusWidget.m tests/suite/TestGaugeWidget.m` exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - Commit graph: 2 commits with message prefix `fix(1014-test): delete legacy-class methods from ...`
  </acceptance_criteria>
  <done>
Both files have zero legacy-class constructor calls, remaining methods pass on MATLAB + Octave, MISS_HIT clean, 2 commits landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete legacy-class methods from TestIconCardWidget.m, TestMultiStatusWidget.m, TestChipBarWidget.m</name>
  <files>tests/suite/TestIconCardWidget.m, tests/suite/TestMultiStatusWidget.m, tests/suite/TestChipBarWidget.m</files>
  <read_first>
    - tests/suite/TestIconCardWidget.m
    - tests/suite/TestMultiStatusWidget.m
    - tests/suite/TestChipBarWidget.m
    - libs/Dashboard/IconCardWidget.m (confirm `.IsUpper` / `.allValues()` dead branches)
    - libs/Dashboard/MultiStatusWidget.m (expected: also reads CompositeThreshold-shaped objects — confirm)
    - libs/Dashboard/ChipBarWidget.m lines 240-290 (RESEARCH cites `.IsUpper` / `.allValues()` here)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md (D-02-A heuristic, D-05 kill-switch)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Pattern 1, Pitfall 1 — Threshold/MonitorTag concept-mapping)
  </read_first>
  <action>
**Enumerated deletion list (from grep run 2026-04-22 — executor uses this list; grep rule below is defense-in-depth).**

**TestIconCardWidget.m — DELETE these 6 methods:**
  - testThresholdBinding (line 134) — uses Threshold()
  - testThresholdKeyResolution (line 142) — uses Threshold()
  - testMutualExclusivity (line 151) — uses Threshold()
  - testDeriveStateFromThreshold (line 166) — uses Threshold()
  - testThresholdWithValueFcn (line 181) — uses Threshold()
  - testSerializeThresholdRoundTrip (line 197) — uses Threshold()
  (KEEP: testDefaultConstruction, testRenderNoError, testRefreshBeforeRender, testToStruct, testFromStruct, testStateColorOk, testStateColorWarn, testStateColorAlarm, testInfoColorInTheme, testInfoColorAllPresets, testStateColorInfo, testStateColorInactive — 12 survivors)

**TestMultiStatusWidget.m — DELETE these 9 methods:**
  - testThresholdStructItem (line 27) — uses Threshold()
  - testThresholdStructColor (line 42) — uses Threshold()
  - testThresholdStructSerialize (line 59) — uses Threshold()
  - testMixedSensorAndThresholdItems (line 74) — uses Threshold()
  - testCompositeExpansion (line 86) — uses Threshold() + CompositeThreshold()
  - testCompositeExpansionMixed (line 103) — uses Threshold() + CompositeThreshold()
  - testCompositeExpansionNestedFlattens (line 122) — uses Threshold() + CompositeThreshold()
  - testCompositeExpansionSummaryColor (line 139) — uses Threshold() + CompositeThreshold()
  - testNonCompositeUnchanged (line 157) — uses Threshold()
  (KEEP: testDefaultConstruction, testToStruct — only 2 survivors. Pitfall 5 guard: both non-legacy, `methods (Test)` block is not empty.)

**TestChipBarWidget.m — DELETE these 3 methods:**
  - testChipThreshold (line 112) — uses Threshold()
  - testChipThresholdWithValueFcn (line 130) — uses Threshold()
  - testChipThresholdSerialize (line 149) — uses Threshold()
  (KEEP: testDefaultConstruction, testRenderThreeChips, testSingleAxes, testRefreshBeforeRender, testToStruct, testFromStruct, testChipColorUpdate — 7 survivors)

Every method named above traces to a CI-log error in `1014-CONTEXT.md §specifics`.

**DEFENSE-IN-DEPTH grep rule (catches anything the enumeration missed):**

Apply the SAME deletion rule as Task 1 to EACH of the three files. Reference Task 1 action for the full procedure. Differences to note for these three files:

**TestMultiStatusWidget.m** — contains `CompositeThreshold(...)` references in addition to plain `Threshold(...)`. Delete methods that use EITHER. The widget's `MultiStatusWidget.m` branches reading `CompositeThreshold`-shaped objects are all dead in v2.0 (the class is gone; no object can flow through).

**TestIconCardWidget.m** — research flagged 6 failing methods. All 6 are expected to instantiate `Threshold(...)`. Delete them. Keep `testConstructor*`, `testRender*`, `testGetType`, `testFromStruct*` (where `fromStruct` is not given a Threshold-struct).

**TestChipBarWidget.m** — only 3 failing methods per the CI log. Delete them. The ChipBar widget has a `ChipBarWidget.m:240-290` block reading `.IsUpper`/`.allValues()`; those paths are dead.

For each file:
  1. Enumerate methods with `grep -nE "^\s*function\s+\w+\s*\(testCase" <file>`. Cross-check against the enumerated list above — if any method matching the deletion pattern is NOT in the list above, delete it too and note the discrepancy in the commit body.
  2. Delete methods whose body uses any deleted class (see Task 1 regex list + `CompositeThreshold`).
  3. Verify `methods (Test)` block is non-empty (Pitfall 5); add a constructor smoke test if it would be empty.
  4. Drop any orphaned private helpers (`methods (Access = private)` entries now unreferenced).
  5. Commit separately (three commits total):
     - `fix(1014-test): delete legacy-class methods from TestIconCardWidget`
     - `fix(1014-test): delete legacy-class methods from TestMultiStatusWidget`
     - `fix(1014-test): delete legacy-class methods from TestChipBarWidget`
  6. Each commit body lists the exact method names deleted.

Per D-05: if any single file > 45 min, delete aggressively (don't attempt migration).
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); for T = {'TestIconCardWidget','TestMultiStatusWidget','TestChipBarWidget'}; runtests(['tests/suite/' T{1} '.m']); end" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - For each of the three files: `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/<file>` returns 0
    - `grep -c "detectEventsFromSensor" tests/suite/TestIconCardWidget.m tests/suite/TestMultiStatusWidget.m tests/suite/TestChipBarWidget.m` returns 0
    - Each file still compiles as a classdef (no empty `methods (Test)` block): `matlab -batch "clear classes; TestIconCardWidget; TestMultiStatusWidget; TestChipBarWidget; disp('ok')"` exits 0
    - `matlab -batch "runtests('tests/suite/TestIconCardWidget.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestMultiStatusWidget.m')" ; echo $?` returns 0
    - `matlab -batch "runtests('tests/suite/TestChipBarWidget.m')" ; echo $?` returns 0
    - `mh_style tests/suite/TestIconCardWidget.m tests/suite/TestMultiStatusWidget.m tests/suite/TestChipBarWidget.m && mh_lint ...` exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - Commit graph: 3 commits with message prefix `fix(1014-test): delete legacy-class methods from ...`
  </acceptance_criteria>
  <done>
All three files have zero legacy-class constructor calls, remaining methods pass, MISS_HIT clean, 3 commits landed. Plan 02 complete — 5 widget test files cleaned.
  </done>
</task>

</tasks>

<verification>
After plan complete:
- Failure count in these 5 widget files drops from ~38 errors to 0.
- Remaining methods in each file pass on MATLAB + Octave.
- Commit graph: 5 commits (one per file per D-04).
- No new lint warnings introduced.
- Plan 03 (`*Tag.m` threshold strip) can proceed in parallel — no file overlap.
</verification>

<success_criteria>
- 5 widget test files cleaned of legacy-class constructor calls.
- All surviving methods pass in MATLAB CI and Octave.
- 5 atomic commits landed.
- MISS_HIT clean.
- SUMMARY.md lists exactly which method names were deleted per file.
</success_criteria>

<output>
Create `.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-02-SUMMARY.md` listing:
- Per-file: method names deleted (for git-log bisectability)
- Per-file: method names kept
- Total methods deleted (expected ~38)
- Confirmation no methods were "migrated" (deletion-only plan per D-02-A default)
- Octave suite green signal
</output>

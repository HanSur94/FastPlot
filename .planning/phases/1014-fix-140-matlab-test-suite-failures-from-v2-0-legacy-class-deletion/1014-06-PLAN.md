---
phase: 1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion
plan: 06
type: execute
wave: 2
depends_on: [1014-01, 1014-02, 1014-03, 1014-04, 1014-05]
files_modified:
  - tests/suite/TestDashboardBugFixes.m
  - tests/suite/TestDashboardBuilderInteraction.m
  - tests/suite/TestDashboardEngine.m
  - tests/suite/TestDashboardPerformance.m
  - tests/suite/TestDashboardToolbarImageExport.m
  - tests/suite/TestDashboardSerializerRoundTrip.m
  - tests/suite/TestDataSource.m
  - tests/suite/TestDataStoreWAL.m
  - tests/suite/TestDatastoreEdgeCases.m
  - tests/suite/TestFastSenseWidget.m
  - tests/suite/TestFastSenseWidgetUpdate.m
  - tests/suite/TestNumberWidget.m
  - tests/suite/TestWebBridge.m
autonomous: true
requirements: [PHASE-1014-GATE]
must_haves:
  truths:
    - "All 13 Dashboard-adjacent files have zero legacy-class constructor calls"
    - "Each file's failing methods are either fixed or deleted; surviving methods pass"
    - "TestDashboardToolbarImageExport status reconciled with Phase 1006-04 fix (if still failing, diagnosed independently)"
    - "TestDashboardBugFixes does NOT re-regress (Plan 01's two fixes preserved)"
    - "Octave function-style suite stays green"
    - "MISS_HIT clean on all edited files"
  artifacts:
    - path: "tests/suite/TestDashboardBugFixes.m"
      provides: "Residual failures beyond Plan 01's 2 methods fixed or deleted"
    - path: "tests/suite/TestDashboardBuilderInteraction.m"
      provides: "5 verify-failures fixed (UI drift updates)"
    - path: "tests/suite/TestDashboardSerializerRoundTrip.m"
      provides: "4 sub-verification failures addressed"
    - path: "tests/suite/TestWebBridge.m"
      provides: "5 failing methods fixed or deleted"
    - path: "tests/suite/TestFastSenseWidget.m"
      provides: "3 failing methods fixed or deleted"
    - path: "tests/suite/TestNumberWidget.m"
      provides: "3 failing methods fixed or deleted"
    - path: "tests/suite/TestDataStoreWAL.m"
      provides: "2 failing methods fixed or deleted"
  key_links:
    - from: "TestDashboardToolbarImageExport"
      to: "libs/Dashboard/DashboardEngine.m:452-468 exportgraphics branch"
      via: "Phase 1006-04 fix — verify still live; if regression, diagnose"
      pattern: "exportgraphics\\("
---

<objective>
Wave 2 small-number Dashboard-adjacent batch. These 13 files each have 1-5 failing methods — the long tail after Wave 1's heavy-hitters. Some are Category A (legacy-class refs); some are Category F (regex drift, UI changes, unclear root cause).

**Strategy (per file):**
1. grep for deleted-class constructor calls. If hit: apply Plan 02 / Plan 03 rules (strip arm or delete method).
2. If no hit: per-method root-cause:
   - UI drift (Phase 1000 perf changes broke `testRepositionPanels`, etc.) → update expected values
   - Schema drift (`TestDashboardSerializerRoundTrip` sub-verifications) → update expected widget property list
   - Regex gate drift → widen regex window or rewrite gate
3. `TestDashboardToolbarImageExport` (4 failures) — RESEARCH says Phase 1006-04 fix is present at `libs/Dashboard/DashboardEngine.m:452-468`. If CI still shows 4 failures: diagnose whether it's a new regression or stale CI log. Do NOT preemptively "fix" what's already fixed.

**Wave 2 ordering rationale:** depends on all Wave 1 plans because some of these files (e.g., `TestFastSenseWidget.m`) may have methods that cross into Wave 1 territory; if a Wave 1 plan left an unexpected residue, Wave 2 catches it.

Purpose: Close the long-tail failures. Output: 13 files updated, failure count for this batch → 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md
@tests/suite/TestDashboardBugFixes.m
@tests/suite/TestDashboardBuilderInteraction.m
@tests/suite/TestDashboardEngine.m
@tests/suite/TestDashboardPerformance.m
@tests/suite/TestDashboardToolbarImageExport.m
@tests/suite/TestDashboardSerializerRoundTrip.m
@tests/suite/TestDataSource.m
@tests/suite/TestDataStoreWAL.m
@tests/suite/TestDatastoreEdgeCases.m
@tests/suite/TestFastSenseWidget.m
@tests/suite/TestFastSenseWidgetUpdate.m
@tests/suite/TestNumberWidget.m
@tests/suite/TestWebBridge.m
@libs/Dashboard/DashboardEngine.m
@libs/Dashboard/DashboardBuilder.m
@libs/FastSense/FastSenseDataStore.m
@libs/WebBridge/WebBridge.m

<interfaces>
<!-- Plan 01 already fixed TestDashboardBugFixes methods testSensorListenersMultiPage + testExitEditModeAfterFigureClose -->
<!-- Any residual TestDashboardBugFixes failure in Wave 2 is beyond those two. -->

<!-- Phase 1006-04 exportgraphics fix: libs/Dashboard/DashboardEngine.m lines 452-468 -->
<!-- RESEARCH expects this to already be present; TestDashboardToolbarImageExport failures may be stale CI log or new regression. -->

<!-- Plans 02-05 already cleaned widget/*Tag/EventDetection files — Wave 2 must not re-edit those. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Diagnose + fix 5 widget/dashboard files (TestDashboardBugFixes residuals, TestDashboardEngine, TestDashboardPerformance, TestFastSenseWidget, TestFastSenseWidgetUpdate, TestNumberWidget)</name>
  <files>tests/suite/TestDashboardBugFixes.m, tests/suite/TestDashboardEngine.m, tests/suite/TestDashboardPerformance.m, tests/suite/TestFastSenseWidget.m, tests/suite/TestFastSenseWidgetUpdate.m, tests/suite/TestNumberWidget.m</files>
  <read_first>
    - tests/suite/TestDashboardBugFixes.m (Plan 01 fixed 2 methods — check what other failure exists; CI log notes 1 residual verify-fail)
    - tests/suite/TestDashboardEngine.m (1 error per CI log)
    - tests/suite/TestDashboardPerformance.m (1 error per CI log)
    - tests/suite/TestFastSenseWidget.m (2 error + 1 verify-fail per CI log)
    - tests/suite/TestFastSenseWidgetUpdate.m (1 error per CI log)
    - tests/suite/TestNumberWidget.m (1 error + 2 verify-fail per CI log)
    - libs/Dashboard/DashboardEngine.m (architecture reference — especially addWidget, onLiveTick, Pages handling)
    - libs/FastSense/FastSense.m (addTag and addSensor paths — FastSenseWidget delegation)
    - libs/Dashboard/NumberWidget.m (ValueFcn and Sensor/Tag binding)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Category F Triage Table)
  </read_first>
  <action>
For EACH of the 6 files, per-method triage. Process in this order: small ones first (TestDashboardEngine, TestDashboardPerformance — 1 method each) → larger (TestNumberWidget, TestFastSenseWidget).

**Step for each file:**

1. Enumerate failing methods (from CI log + local `runtests(<file>)`).
2. For each failing method, grep the body for deleted-class calls:
```
grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" <method_body>
```
3. If hit → apply Plan 02/03 rules:
   - Plain `Sensor(...)` construction fed to widget → migrate to `SensorTag(...)` IF widget's Tag path works.
   - `Threshold(...)` / `CompositeThreshold(...)` → DELETE method (D-02-A default).
4. If no legacy hit → diagnose independently:
   - Is the assertion value hardcoded (e.g., `verifyEqual(numel(widgets), 7)`) and the count changed because of a new feature? → update the expected value.
   - Is it testing a contract that moved (e.g., `DashboardEngine.render` now calls `allocatePanels` where it used to inline)? → update the test to observe the new contract.
   - Is it an Octave-only or R2020b-only edge case? → guard with `if exist('OCTAVE_VERSION', 'builtin'), testCase.assumeFail(...); end`.

**Specific known cases (from RESEARCH Category F triage table + CONTEXT.md):**

- `TestDashboardBugFixes` residual (1 verify-fail): grep for deleted-class refs; most likely a small UI drift. Fix or delete.
- `TestFastSenseWidget/*` (3 failures): FastSenseWidget in Phase 1009 gained a base-class `Tag` property. Legacy Sensor assertions may need renaming. If a method asserts `w.Sensor.Key` where v2.0 uses `w.Tag.Key`, update the assertion.
- `TestNumberWidget/*` (3 failures): NumberWidget has `ValueFcn` + `Sensor`/`Tag` binding. Methods asserting `w.Sensor.X = newVal` directly: if the test uses `Sensor(...)` constructor → DELETE; if it uses plain struct, update to `SensorTag`.

**Pitfall 5:** ensure `methods (Test)` non-empty after deletions.

**Commit discipline (D-04):** one commit per file:
  - `fix(1014-test): diagnose+fix TestDashboardBugFixes residual`
  - `fix(1014-test): diagnose+fix TestDashboardEngine`
  - `fix(1014-test): diagnose+fix TestDashboardPerformance`
  - `fix(1014-test): diagnose+fix TestFastSenseWidget`
  - `fix(1014-test): diagnose+fix TestFastSenseWidgetUpdate`
  - `fix(1014-test): diagnose+fix TestNumberWidget`

Each commit body documents: ROOT CAUSE + FIX (whether delete or migrate or assertion update).

**D-05:** avg 5-10 min per file; if any single file > 30 min, apply aggressive deletion.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); for T = {'TestDashboardBugFixes','TestDashboardEngine','TestDashboardPerformance','TestFastSenseWidget','TestFastSenseWidgetUpdate','TestNumberWidget'}; runtests(['tests/suite/' T{1} '.m']); end" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 6 files: `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/<file>` returns 0
    - `grep -c "detectEventsFromSensor" tests/suite/TestDashboardBugFixes.m tests/suite/TestDashboardEngine.m tests/suite/TestDashboardPerformance.m tests/suite/TestFastSenseWidget.m tests/suite/TestFastSenseWidgetUpdate.m tests/suite/TestNumberWidget.m` returns 0
    - Each file: `matlab -batch "runtests('tests/suite/<file>')" ; echo $?` returns 0
    - `mh_style` + `mh_lint` on the 6 files exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - Plan 01's `testSensorListenersMultiPage` + `testExitEditModeAfterFigureClose` STILL pass (no regression): `matlab -batch "runtests('tests/suite/TestDashboardBugFixes.m/testSensorListenersMultiPage'); runtests('tests/suite/TestDashboardBugFixes.m/testExitEditModeAfterFigureClose')" ; echo $?` returns 0
    - 6 commits landed with `fix(1014-test):` prefix
  </acceptance_criteria>
  <done>
6 widget/dashboard files fixed, each commit documents root cause + fix, MISS_HIT clean, Octave green, Plan 01 fixes preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Diagnose + fix remaining 7 files (TestDashboardBuilderInteraction, TestDashboardSerializerRoundTrip, TestDashboardToolbarImageExport, TestDataSource, TestDataStoreWAL, TestDatastoreEdgeCases, TestWebBridge)</name>
  <files>tests/suite/TestDashboardBuilderInteraction.m, tests/suite/TestDashboardSerializerRoundTrip.m, tests/suite/TestDashboardToolbarImageExport.m, tests/suite/TestDataSource.m, tests/suite/TestDataStoreWAL.m, tests/suite/TestDatastoreEdgeCases.m, tests/suite/TestWebBridge.m</files>
  <read_first>
    - tests/suite/TestDashboardBuilderInteraction.m (5 verify-fails; RESEARCH: UI layer drift — Phase 1000 performance work changed render() ordering)
    - tests/suite/TestDashboardSerializerRoundTrip.m (1 method × 4 sub-verifications; RESEARCH: widget schema drift)
    - tests/suite/TestDashboardToolbarImageExport.m (4 errors — BUT Phase 1006-04 fix is present in DashboardEngine.m:452-468; diagnose whether it's stale CI or regression)
    - tests/suite/TestDataSource.m (1 verify-fail)
    - tests/suite/TestDataStoreWAL.m (2 errors)
    - tests/suite/TestDatastoreEdgeCases.m (1 error)
    - tests/suite/TestWebBridge.m (5 errors)
    - libs/Dashboard/DashboardEngine.m lines 440-500 (exportImage / exportgraphics branch)
    - libs/Dashboard/DashboardBuilder.m (after Plan 01's edits — exitEditMode guarded)
    - libs/Dashboard/DashboardSerializer.m (current widget-property schema)
    - libs/FastSense/FastSenseDataStore.m (WAL-mode entry points)
    - libs/WebBridge/WebBridge.m (TCP / python subprocess wiring — may have drifted)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Category F Triage Table — each of these files has an expected root cause)
  </read_first>
  <action>
Per-file triage, same rule as Task 1 (grep deleted-class → delete/migrate; else root-cause per method).

**TestDashboardToolbarImageExport.m (4 errors) — priority diagnosis:**
1. grep `libs/Dashboard/DashboardEngine.m` lines 452-468 for `exportgraphics` — confirm the Phase 1006-04 fix is still present:
   ```
   grep -n "exportgraphics" libs/Dashboard/DashboardEngine.m
   ```
2. If present: run the test locally (or via Octave docker proxy) — if it passes, the CI log is STALE and the file needs no edit.
3. If still failing: diagnose the NEW regression. Likely causes: (a) CI environment lost headless display setup; (b) a commit since Phase 1006-04 removed the `useExportGraphics` branch; (c) test itself has a bug in its assertion.
4. DO NOT add a library fix unless absolutely necessary — CONTEXT.md D-05 says library cascades beyond DashboardBuilder.m require STOP+discuss.
5. If library fix needed → escalate to user (this plan pauses), do not silently land.

**TestDashboardBuilderInteraction.m (5 verify-fails):**
- RESEARCH: UI drift from Phase 1000 performance work.
- Likely: hardcoded expected panel counts, render-order assertions, ordering of `realizeBatch` calls.
- Per method: update expected values to match current `DashboardEngine.render()` + `DashboardBuilder` flow.
- Octave fixture tests exercise same flow — check `tests/test_dashboard_builder_interaction.m` passes too after changes.

**TestDashboardSerializerRoundTrip.m (1 method × 4 sub-verifications):**
- RESEARCH: widget-property schema drift.
- The `testRoundTripPreservesWidgetSpecificProperties` asserts that `save + load` preserves specific widget props. Some widgets gained new props in Phases 1007/1009/1010 (`ShowThresholdLabels`, `Tag`, `YLimits`, etc.); others lost props.
- Per sub-verification: read the failing assertion, find the widget + prop, confirm current toStruct/fromStruct behaviour in `libs/Dashboard/<Widget>.m`, update the expected value.

**TestDataStoreWAL.m (2 errors), TestDatastoreEdgeCases.m (1 error):**
- grep for deleted-class refs; most likely legacy `Sensor(...)` constructor fed into DataStore. Replace with plain arrays or `SensorTag` depending on DataStore API signature (read `libs/FastSense/FastSenseDataStore.m` constructor).

**TestDataSource.m (1 verify-fail):**
- RESEARCH gives no expected cause; diagnose from the single failing method.

**TestWebBridge.m (5 errors):**
- grep for deleted-class refs first. If hit → Plan 02/03 rules.
- If not: WebBridge depends on Dashboard + FastSenseDataStore schemas; likely WebBridgeProtocol format drift or TCP port/subprocess issue. Diagnose per method.
- If WebBridge fix requires Python-side change → **DELETE the failing method per D-02-A** (MATLAB-only test suite cannot verify Python-side behaviour without the CI WebBridge fixture; a test that can only be validated by Python changes is a test of deleted MATLAB behaviour by proxy). Do NOT edit `bridge/python/` or any Python file — Phase 1014 scope is `tests/suite/*.m` only (D-01). Document the deletion in the commit body: "DELETE <method> — asserted Python-side WebBridge behaviour; no MATLAB-only verification path; per D-02-A / D-01 scope."

**Commit discipline (D-04):** one commit per file:
  - `fix(1014-test): TestDashboardBuilderInteraction — update UI-drift expectations`
  - `fix(1014-test): TestDashboardSerializerRoundTrip — update widget schema expectations`
  - `fix(1014-test): TestDashboardToolbarImageExport — reconcile with Phase 1006-04 fix`
  - `fix(1014-test): TestDataSource — fix residual verify-failure`
  - `fix(1014-test): TestDataStoreWAL — fix DataStore WAL test errors`
  - `fix(1014-test): TestDatastoreEdgeCases — fix edge-case test error`
  - `fix(1014-test): TestWebBridge — fix/delete legacy WebBridge methods`

Each commit body: ROOT CAUSE + FIX summary.

**D-05:** target avg 10-15 min per file. If any file > 30 min, aggressive delete.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); for T = {'TestDashboardBuilderInteraction','TestDashboardSerializerRoundTrip','TestDashboardToolbarImageExport','TestDataSource','TestDataStoreWAL','TestDatastoreEdgeCases','TestWebBridge'}; runtests(['tests/suite/' T{1} '.m']); end" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 7 files: `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/<file>` returns 0
    - Each file: `matlab -batch "runtests('tests/suite/<file>')" ; echo $?` returns 0
    - `mh_style` + `mh_lint` on the 7 files exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - NO libs/ changes beyond what Plan 01 authorized (DashboardBuilder.m); if ToolbarImageExport required a lib change, the plan escalated and this verification catches a scope violation: `git diff --name-only | grep -E "^libs/"` returns empty
    - 7 commits landed
  </acceptance_criteria>
  <done>
7 remaining files triaged and fixed, MISS_HIT clean, Octave green, no unauthorized libs/ changes, 7 commits landed.
  </done>
</task>

</tasks>

<verification>
- All 13 Wave 2 files pass on MATLAB + Octave.
- 13 atomic commits landed.
- Zero legacy-class constructor calls across all 13 files.
- Plan 01's two Category E fixes preserved.
- No libs/ changes beyond DashboardBuilder.m.
- MISS_HIT clean.
</verification>

<success_criteria>
- ~25 erroring/verify-failing methods closed.
- 13 atomic commits landed.
- SUMMARY documents per-file: ROOT CAUSE + FIX (delete/migrate/assertion-update).
- D-05 kill-switch outcomes documented where fired.
</success_criteria>

<output>
Create `.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-06-SUMMARY.md` listing per-file ROOT CAUSE + FIX + surviving-method counts + confirmation that libs/ changes stayed within Plan 01's scope.
</output>

---
phase: 1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion
plan: 04
type: execute
wave: 1
depends_on: [1014-01]
files_modified:
  - tests/suite/TestSensorDetailPlot.m
autonomous: true
requirements: [PHASE-1014-GATE]
must_haves:
  truths:
    - "TestSensorDetailPlot.m has zero legacy-class constructor calls (Sensor, Threshold, ThresholdRule, StateChannel, SensorRegistry, ThresholdRegistry, ExternalSensorRegistry, CompositeThreshold)"
    - "Surviving methods pass on MATLAB R2020b and Octave"
    - "TestData.sensor migration from Plan 01 is preserved"
    - "D-05 kill-switch invoked if plan exceeds 45 min — aggressive deletion over migration"
    - "MISS_HIT lint clean"
  artifacts:
    - path: "tests/suite/TestSensorDetailPlot.m"
      provides: "SensorDetailPlot tests migrated or pruned to v2.0 Tag API"
      contains: "classdef TestSensorDetailPlot"
  key_links:
    - from: "Surviving test methods"
      to: "SensorDetailPlot Tag-binding live paths"
      via: "SensorTag + MonitorTag fixtures replacing Sensor + Threshold"
      pattern: "SensorTag\\("
---

<objective>
Wave 1 heavy-hitter. `TestSensorDetailPlot.m` has 21 erroring methods — the largest single-file offender in Phase 1014. Research calls it the #1 D-05 budget risk.

**Strategy:**
1. Plan 01 (Wave 0) already migrated `testCase.TestData.sensor` → `testCase.sensor` (private property). DO NOT revisit that migration; build on it.
2. The file contains a static helper `createTagWithThreshold` (per RESEARCH) that legacy-instantiates `Threshold(...)` and entangles many methods. Strip that helper first — it cascades cleanup.
3. Apply per-method triage (same rule as Plans 02/03):
   - Method exercises a Tag path where the widget/library actually has live code → MIGRATE to `SensorTag` + `MonitorTag` (use `MakePhase1009Fixtures`).
   - Method exercises only deleted behaviour → DELETE.
4. Keep pure render/navigation/zoom tests that don't touch legacy classes at all — they should already pass post-Plan-01.

**D-05 kill-switch (HIGH LIKELIHOOD for this plan):** RESEARCH ranks this plan #1 risk for blowing the 45-min budget. If at any point you've spent > 45 min on migration attempts, SWITCH to pure deletion for all remaining ambiguous methods. Keep 6-8 survivors covering render / navigation / zoom. A missing test is recoverable; a bad migration is not.

Purpose: Close the 21-error cliff and unblock the overall CI gate. Output: 1 file with zero legacy-class refs, green on R2020b + Octave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md
@.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md
@tests/suite/TestSensorDetailPlot.m
@tests/suite/TestSensorDetailPlotTag.m
@tests/suite/TestCompositeTag.m
@tests/suite/MakePhase1009Fixtures.m
@libs/FastSense/SensorDetailPlot.m
@libs/SensorThreshold/SensorTag.m
@libs/SensorThreshold/MonitorTag.m

<interfaces>
<!-- After Plan 01, the TestSensorDetailPlot fixture already uses `testCase.sensor` -->
<!-- Replacement constructors: -->
Sensor('key', 'Name', name)                    → SensorTag('key', 'Name', name)
Sensor('key', 'Name', n, 'X', xv, 'Y', yv)     → SensorTag('key', 'Name', n, 'X', xv, 'Y', yv)
s.addThreshold(Threshold(...))                 → DELETE — SensorTag has no .Thresholds{}; MonitorTag is a sibling, not a child
Threshold('k', 'Direction', 'upper'); t.addCondition(struct(), V)
                                               → MonitorTag('k', parentSensorTag, @(x,y) y > V)
                                                 OR DELETE the method entirely
StateChannel('k', X, Y)                        → StateTag('k', 'X', X, 'Y', Y)

<!-- SensorDetailPlot's live Tag path (libs/FastSense/SensorDetailPlot.m) -->
Constructor may accept `'Tag', aSensorTag` NV pair — confirm exact signature in the file before migrating.

<!-- Golden fixtures -->
MakePhase1009Fixtures.makeSensorTag()     — SensorTag with known Y pattern
MakePhase1009Fixtures.makeMonitorTag()    — default y > 15 MonitorTag
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip createTagWithThreshold helper + bulk-triage 21 failing methods</name>
  <files>tests/suite/TestSensorDetailPlot.m</files>
  <read_first>
    - tests/suite/TestSensorDetailPlot.m (the file — Plan 01's TestData.sensor migration should already be applied; confirm before starting)
    - tests/suite/TestSensorDetailPlotTag.m (Plan 03 is handling this one — peek to understand the live Tag patterns but DO NOT edit)
    - tests/suite/TestCompositeTag.m (green reference for Tag-API idioms)
    - tests/suite/MakePhase1009Fixtures.m lines 23-60 (makeSensorTag + makeMonitorTag)
    - libs/FastSense/SensorDetailPlot.m (confirm the Tag binding NV pair signature before migrating)
    - libs/SensorThreshold/SensorTag.m (constructor signature)
    - libs/SensorThreshold/MonitorTag.m (constructor + ConditionFn semantics)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-CONTEXT.md (D-02-A + D-05 kill-switch)
    - .planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-RESEARCH.md (Plan 04 risk note — HIGH risk of D-05 fire)
  </read_first>
  <action>
**Start a wall-clock timer.** If > 45 minutes elapse, invoke D-05 kill-switch: delete all remaining ambiguous methods and wrap up.

**Pre-triage table (from reading `tests/suite/TestSensorDetailPlot.m` at planning time, 2026-04-22):**

The file has 21 test methods total. All 21 are "failing" in the sense that Plan 01 migrates `testCase.TestData.sensor` → `testCase.sensor` — every method transitively fails until that migration lands. After Plan 01, the residual failures cluster around the static helper `createTagWithThreshold` at lines 316-328, which instantiates the deleted `Threshold(...)` class. That helper is called by 4 methods.

| # | Method (line) | Disposition | Rationale |
|---|---------------|-------------|-----------|
| 1 | testConstructorStoresTag (26) | MIGRATE | Pure TestData.sensor fixture; Plan 01 rename → passes as-is. Verify post-Plan-01. |
| 2 | testConstructorDefaultOptions (32) | MIGRATE | Same — reads `sdp.NavigatorHeight` / `ShowThresholds`; no legacy calls. |
| 3 | testConstructorCustomOptions (41) | MIGRATE | Same — NV-pair construction; no legacy calls. |
| 4 | testRenderCreatesMainAndNavigator (53) | MIGRATE | Pure render check against `?FastSense` class; no legacy calls. |
| 5 | testRenderTwiceThrows (62) | MIGRATE | `verifyError('SensorDetailPlot:alreadyRendered')`; no legacy calls. |
| 6 | testMainPlotHasSensorLine (70) | MIGRATE | `verifyGreaterThanOrEqual(numel(sdp.MainPlot.Lines), 1)`; no legacy calls. |
| 7 | testNavigatorHasDataLine (78) | MIGRATE | Same pattern; no legacy calls. |
| 8 | testSetGetZoomRange (86) | MIGRATE | Zoom API check; no legacy calls. |
| 9 | testThresholdsShownWhenEnabled (97) | DELETE | Calls `createTagWithThreshold` → dead legacy Threshold path. SensorDetailPlot's `MainPlot.Thresholds` array is populated only from `Sensor.Thresholds{}` which SensorTag always returns `{}`. |
| 10 | testThresholdsHiddenWhenDisabled (105) | DELETE | Same as #9 — helper path is dead. |
| 11 | testNavigatorHasThresholdBands (114) | DELETE | Same — helper + `Bands` array populated from deleted Threshold branch. |
| 12 | testNavigatorNoBandsWhenDisabled (122) | DELETE | Same — helper path dead. |
| 13 | testEventShadingInMainPlot (131) | MIGRATE | Uses `Event(...)` (still live in v2.0); no legacy Threshold/Sensor calls. |
| 14 | testEventLinesInNavigator (156) | MIGRATE | Same — `Event(...)` only. |
| 15 | testEventsFromEventstore (180) | MIGRATE | Uses `EventStore` + `Event(...)` — both live in v2.0. |
| 16 | testEventColorHigh (210) | MIGRATE | `Event(...)` + patch color check; no legacy calls. |
| 17 | testEventColorEscalated (232) | MIGRATE | Same pattern. |
| 18 | testEventPatchUserdataFields (257) | MIGRATE | Uses `Event(...) + ev.setStats(...)`; no legacy calls. |
| 19 | testTilePanelReturnsUipanel (288) | MIGRATE | `FastSenseGrid` tile check; no legacy calls. |
| 20 | testTilePanelConflictWithTile (295) | MIGRATE | Same. |
| 21 | testEmbeddedInFigureTile (303) | MIGRATE | `FastSenseGrid` + SensorDetailPlot render; no legacy calls. |

**Summary:** 4 DELETE (#9-12 — all call `createTagWithThreshold`), 17 MIGRATE (mostly a no-op after Plan 01's TestData rename; just re-run to confirm).

**Private helper `createTagWithThreshold` (lines 316-328):** DELETE this helper entirely. It is called only by methods #9-12, all of which are deleted. Grep `grep -n createTagWithThreshold tests/suite/TestSensorDetailPlot.m` — expect 4 call sites post-deletion → 0.

**Step-by-step procedure:**

STEP 1 — Confirm Plan 01's Category C migration landed:
```
grep -c "testCase\.TestData" tests/suite/TestSensorDetailPlot.m   # must be 0
grep -c "properties (Access = private)" tests/suite/TestSensorDetailPlot.m   # must be >= 1
```
If either fails, STOP and verify Plan 01 shipped correctly.

STEP 2 — Delete the 4 methods flagged DELETE above (#9-12). Delete the private static helper `createTagWithThreshold` (lines 316-328). Post-delete grep check:
```
grep -c "createTagWithThreshold\|Threshold(" tests/suite/TestSensorDetailPlot.m   # must be 0
```

STEP 3 — For the 17 MIGRATE methods: they should already pass post-Plan-01. Run the file:
```
matlab -batch "runtests('tests/suite/TestSensorDetailPlot.m')"
```
If any MIGRATE method STILL fails after Plan 01 + the 4 deletions, diagnose individually:
  - If the root cause is a deleted-class reference the table missed → DELETE the method (defense in depth); note in commit body as "enumeration miss".
  - If the root cause is something else (e.g. `SensorDetailPlot` API drift) → triage per CONTEXT.md D-02-F; fix or delete.
  - D-05 kill-switch: if migration of any individual method exceeds 45 min wall-clock total, DELETE remaining ambiguous methods rather than continue.

STEP 4 — Verify `methods (Test)` contains >= 1 method (Pitfall 5). After 4 deletions, 17 survivors remain — safe.

STEP 5 — Clean orphaned private helpers: `createTagWithThreshold` is the only one; confirm no other orphans via `grep -nE "^\s*function" tests/suite/TestSensorDetailPlot.m` followed by per-function call-site check.

STEP 6 — Commit: `fix(1014-test): TestSensorDetailPlot — delete 4 createTagWithThreshold callers + helper (Plan 04)`. Commit body enumerates:
  - Methods DELETED: testThresholdsShownWhenEnabled, testThresholdsHiddenWhenDisabled, testNavigatorHasThresholdBands, testNavigatorNoBandsWhenDisabled (all 4 exercised the dead SensorTag.Thresholds {} path via the legacy `Threshold(...)` helper)
  - Helper DELETED: createTagWithThreshold (static, private)
  - Methods MIGRATED: the 17 survivors (named, no body changes — Plan 01's TestData rename suffices)
  - D-05 kill-switch status: not-invoked (this plan is bounded by the table) OR invoked (if a MIGRATE method blew up; name the method)

STEP 7 — DO NOT touch `TestSensorDetailPlotTag.m` — Plan 03 owns that file.

**Optional split:** if during execution you discover the MIGRATE batch has unexpected per-method issues (more than 2 of the 17 surface post-Plan-01 failures), STOP and convert this plan into Task 1 (DELETE batch — mechanical, the 4 methods + helper) and Task 2 (MIGRATE fixes — per-method diagnosis). Re-plan via `/gsd:plan-phase --gaps` if needed. Default path: single-task execution per the table.
  </action>
  <verify>
    <automated>matlab -batch "addpath('scripts'); install(); runtests('tests/suite/TestSensorDetailPlot.m')" ; echo EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\b(Threshold|ThresholdRule|CompositeThreshold|Sensor|StateChannel|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\s*\(" tests/suite/TestSensorDetailPlot.m` returns 0
    - `grep -c "detectEventsFromSensor" tests/suite/TestSensorDetailPlot.m` returns 0
    - `grep -c "testCase\.TestData" tests/suite/TestSensorDetailPlot.m` returns 0 (Plan 01 migration preserved)
    - `matlab -batch "runtests('tests/suite/TestSensorDetailPlot.m')" ; echo $?` returns 0
    - File compiles as classdef: `matlab -batch "clear classes; TestSensorDetailPlot; disp('ok')"` exits 0
    - `methods (Test)` block is non-empty: `grep -A1 "methods (Test)" tests/suite/TestSensorDetailPlot.m | grep -cE "function test"` returns >= 1
    - `mh_style tests/suite/TestSensorDetailPlot.m && mh_lint tests/suite/TestSensorDetailPlot.m && mh_metric --ci tests/suite/TestSensorDetailPlot.m` exits 0
    - `octave --eval "cd tests; run_all_tests()" ; echo $?` returns 0
    - Commit `fix(1014-test): TestSensorDetailPlot — Category A migration + deletions (Plan 04)` landed with detailed body enumerating deletions and migrations.
  </acceptance_criteria>
  <done>
TestSensorDetailPlot.m has zero legacy-class refs, surviving methods pass, file compiles, MISS_HIT clean, 1 commit landed. D-05 kill-switch status (invoked or not) documented in SUMMARY.md.
  </done>
</task>

</tasks>

<verification>
- TestSensorDetailPlot.m clean; 21 erroring methods are now either fixed or deleted.
- Octave suite remains green.
- MISS_HIT clean.
- Plan summary documents D-05 kill-switch outcome.
</verification>

<success_criteria>
- 1 file updated, 1 commit landed.
- Zero legacy-class constructor calls remain.
- All surviving methods pass MATLAB R2020b + Octave.
- D-05 budget status documented (either "landed in < 45 min" or "kill-switch invoked at method X, N remaining methods deleted").
</success_criteria>

<output>
Create `.planning/phases/1014-fix-140-matlab-test-suite-failures-from-v2-0-legacy-class-deletion/1014-04-SUMMARY.md`:
- Method-level breakdown: names DELETED / MIGRATED / UNTOUCHED
- D-05 outcome: invoked (when, why, count of methods that got deletion-only path) OR landed in budget
- Pre/post method count and pass count
- Confirmation TestSensorDetailPlotTag.m was not touched (Plan 03 scope)
</output>
